# P8 操作・連携 実装仕様書

**対象フェーズ:** v1.0 製品 / P8 操作・連携（設計書 §18）
**対象読者:** Rust ジュニアエンジニア（MCP・非同期は §9 で補う）
**親文書:** `../構造計算一貫プログラム_実装設計書.md`（以降「設計書」。`§x.y` はその章番号）
**先行フェーズ:** [P0](P0_基盤.md)〜[P7](P7_二次設計.md)（特に P2/P5/P6 解析・P3 編集コマンドを公開）
**前提環境:** Rust stable 1.8x 以降 / `rmcp = "1"`（現行 1.7 系）/ `tokio` / `schemars`

---

## 0. このフェーズについて

### 0.1 目的

外部（AI エージェント・他ツール）からモデル操作・解析実行・結果取得・断面算定を行える
**MCP サーバ**を公開し、**ST-Bridge** で他社一貫プログラム／BIM と相互運用する。GUI も拡充する。
設計書 §13 / §12.5 / §14。

- **squid-n-mcp（rmcp）**：ツール群（model/analysis/result/design/report）、**非同期ジョブ**（投入＋進捗）、
  型安全スキーマ（schemars）、stdio/HTTP。書込系は**破壊的操作を識別子付き**で返す。
- **squid-n-io**：ST-Bridge（XML 2.0 系）インポート／エクスポート（幾何・断面・部材・荷重まで）。
- **squid-n-app**：GUI 拡充（解析種別の網羅・結果ビューの充実）。

### 0.2 完了像（ゴール）

設計書 §18 P8 完了基準「**一連の操作・相互運用**」を次で判定:

1. **MCP Inspector** からモデル読込→解析→結果取得が一連で動作（§8.1）。
2. ツール**スキーマがクライアントに正しく提示**される（schemars。§8.1）。
3. **非同期ジョブ**：解析を投入→進捗通知→完了後に結果取得（§8.2）。
4. **ST-Bridge** 代表モデルが往復（import→export）で**意味的に保存**される（§8.3）。

### 0.3 スコープ境界（含む／含まない）

| 項目 | P8 で | 備考 |
|---|---|---|
| MCP サーバ（rmcp）・ツール群 | **含む** | §13 |
| 非同期ジョブ（投入＋進捗 notification/resource） | **含む** | §13 |
| 書込系の破壊的操作識別子・確認前提レスポンス | **含む** | §13 |
| 編集は `EditCommand` 経由・単一ライタ | **含む** | P3 §3.3 / R29 |
| schemars 型安全スキーマ | **含む** | §13 |
| ST-Bridge（XML 2.0）入出力（幾何/断面/部材/荷重） | **含む** | §12.5。完全往復は非保証 |
| GUI 拡充 | **含む** | §14 |
| IFC 連携 | **含まない → 将来** | §12.5（必要なら検討） |
| ST-Bridge の解析結果・独自属性の往復 | **含まない** | §12.5 期待値（幾何中心） |
| 認証・マルチユーザ・権限 | **含まない → 将来** | 単一ライタ前提 |

---

## 1. タスク一覧と依存順序

> **UI 関連タスク（[UI設計 §9.3](UI設計.md)）:** 本フェーズで UI-14（断面編集・複製の `EditCommand` を
> MCP `model.edit` からも生成＝GUI と共通経路）と UI-15（CSV 入出力・解析の非同期ジョブを GUI から起動・
> 進捗表示）も実装。


```
T0 squid-n-mcp 雛形（rmcp サーバ・stdio/HTTP）(§2)
   ├─> T1 読取系ツール（model.query / result.get）(§3)
   ├─> T2 書込系ツール（model.edit / model.save、単一ライタ）(§4)
   └─> T3 非同期ジョブ（analysis.run → job → 進捗 → 結果）(§5)
          └─> T4 design.check / report.export ツール (§6)
T5 ST-Bridge 入出力（squid-n-io）(§7)
T6 GUI 拡充（squid-n-app）(§8?→§7.5)
T0..T6 ─> T7 テスト・DoD (§8)
```

| ID | タスク | クレート | 依存 |
|---|---|---|---|
| T0 | squid-n-mcp 雛形（rmcp） | squid-n-mcp（新規） | P2 |
| T1 | 読取系ツール | squid-n-mcp | T0 |
| T2 | 書込系ツール（単一ライタ） | squid-n-mcp | T0, P3 §3.3 |
| T3 | 非同期ジョブ | squid-n-mcp | T0, P2/P5/P6 |
| T4 | design/report ツール | squid-n-mcp | T3, P7 |
| T5 | ST-Bridge 入出力 | squid-n-io | P0 §4 |
| T6 | GUI 拡充 | squid-n-app | P3 |
| T7 | テスト・DoD | 全体 | 上記 |

---

## 2. T0: squid-n-mcp 雛形（rmcp）

設計書 §13。**rmcp 1.7 の実 API。** 下記は `crates/squid-n-mcp/src/lib.rs`（実装済み）と一致させてある。

> **rmcp 1.7 の実 API に合わせ訂正（2026-07）。** 旧版の本節は「確認済み・そのまま使える」としていたが、
> `ToolRouter`/`Parameters` の再エクスポート先や `ServerInfo` の組み立て方が実際の 1.7 系とは異なっていた
> （下記コード例参照）。以下は実装（`crates/squid-n-mcp/src/lib.rs`）で動作確認済みの形。

確定しているパターン（rmcp 1.7、`features = ["server","macros","transport-io"]`）:
- ツール定義：`impl` ブロックに `#[tool_router]`、各メソッドに `#[tool(description="…")]`。
  メソッドは `pub async fn`、引数は `Parameters(args): Parameters<引数型>`、戻り値は
  `Result<CallToolResult, rmcp::ErrorData>`。
- サーバ実装：`#[tool_handler] impl ServerHandler for ...`（`get_info` で `ServerInfo` を返す）。
- 起動：`use rmcp::ServiceExt;` の `.serve(stdio()).await?` → 返った service を `.waiting().await?`。
  `stdio()` は `rmcp::transport::stdio`。HTTP は `transport` の streamable-http（feature 追加）。

```rust
// squid-n-mcp/src/lib.rs
use rmcp::handler::server::tool::ToolRouter;
// `Parameters` は `handler::server::tool` 内では private re-export のため、
// 実体を持つ `handler::server::wrapper` から取る（1.7 で確認済み）。
use rmcp::handler::server::wrapper::Parameters;
use rmcp::model::{CallToolResult, Content, Implementation, ServerInfo};
use rmcp::transport::stdio;
use rmcp::{tool, tool_handler, tool_router, ErrorData, ServerHandler, ServiceExt};
use std::sync::Arc;
use tokio::sync::Mutex;

#[derive(Clone)]
pub struct SquidNServer {
    /// 単一ライタ：モデルは1つの所有者が直列に編集（R29）。Mutex で直列化。
    state: Arc<Mutex<ServerState>>,
    tool_router: ToolRouter<Self>,   // #[tool_router] が要求する生成フィールド
}
pub struct ServerState {
    pub model: squid_n_core::model::Model,
    pub undo: squid_n_edit::UndoStack,          // 編集コマンド（P3 §3.3）。下記 layering 注意
    pub jobs: JobRegistry,                 // §5
    pub results: Box<dyn squid_n_io::results::ResultStore>, // P2 §6
}

impl SquidNServer {
    pub fn new(state: ServerState) -> Self {
        Self { state: Arc::new(Mutex::new(state)), tool_router: Self::tool_router() }
    }
}

#[tool_router]
impl SquidNServer {
    // 各ツールは §3/§4/§5/§6 で #[tool] を付けて定義する（new() とは別の impl ブロックにする）。
}

#[tool_handler]
impl ServerHandler for SquidNServer {
    fn get_info(&self) -> ServerInfo {
        // `ServerInfo`（`InitializeResult`）・`Implementation` は #[non_exhaustive] のため
        // 構造体リテラルでは組み立てられない。ビルダーメソッド経由で名称等を設定する。
        ServerInfo::default().with_server_info(Implementation::new("squid-n-mcp", "0.1.0"))
    }
}

// 起動（main 側、#[tokio::main]）:
//   let service = SquidNServer::new(init).serve(stdio()).await?;
//   service.waiting().await?;
```

> **エラー応答の注意（`CallToolResult::error` は使わない）:** rmcp 1.x の `CallToolResult::error` は
> `content`（人間可読なエラーメッセージ）のみを組み立てるヘルパで、JSON-RPC レベルのエラー（例:
> 「ジョブが見つからない」）は表現できない。ツール未検出・引数不正等は
> `Err(ErrorData::invalid_params("...", None))` のように `Result` の `Err` 側で返す
> （§5 の `analysis.status` 実装を参照）。

> **import 行について:** `#[tool]`/`#[tool_router]`/`#[tool_handler]`・`CallToolResult`・`serve`/`stdio`
> の関数名・属性は 1.7 で確定。`ToolRouter`/`Parameters` の**再エクスポートパス**は上記コード例のとおり
> （`ToolRouter` は `handler::server::tool`、`Parameters` は `handler::server::wrapper`）。
> さらに版が上がって差異が出た場合はコンパイラの指示に従って use を直す（調査ではなく機械作業）。

> **⚠ layering 修正（P3 からの調整・必須）:** P3 では `EditCommand`/`UndoStack` を `squid-n-app` に置いたが、
> `squid-n-mcp` と `squid-n-app` は**同じ操作層のピア**であり、`squid-n-mcp → squid-n-app` 依存は `xtask check-deps`
> （P0 §2.5）違反になる。よって **`EditCommand`/`UndoStack` を共有下位クレート（`squid-n-core` 直上の
> `squid-n-edit`、あるいは `squid-n-core` 内）へ移し**、`squid-n-app`・`squid-n-mcp` の双方がそこに依存する形にする。
> P3 のコードはこの移設に合わせて参照先を変える（GUI と MCP が同一の編集経路＝単一ライタを共有）。

**DoD（T0）:** stdio で rmcp サーバが起動し、MCP Inspector から `tools/list` が取得できる。

---

## 3. T1: 読取系ツール

設計書 §13。`schemars` で入出力スキーマを型安全に生成。

```rust
// squid-n-mcp/src/tools_read.rs（§2 と同じ impl ブロック内に #[tool] で追加）
// model.load / model.query / result.get

#[derive(schemars::JsonSchema, serde::Deserialize)]
pub struct QueryArgs { pub kind: String /* node/member/section */, pub filter: Option<String> }
#[derive(schemars::JsonSchema, serde::Serialize)]
pub struct QueryResult { pub items: Vec<serde_json::Value> }

#[tool_router]   // §2 の #[tool_router] impl（new() とは別ブロック）にまとめる（ここでは抜粋）
impl SquidNServer {
    #[tool(description = "節点・部材・断面を検索する")]
    pub async fn model_query(&self, Parameters(args): Parameters<QueryArgs>)
        -> Result<CallToolResult, ErrorData>
    {
        let st = self.state.lock().await;                 // 読み取りロック
        let result = /* st.model から args.kind/filter で収集 */ QueryResult { items: vec![] };
        // 確定 API: 構造化結果は Content::json、テキストは Content::text
        Ok(CallToolResult::success(vec![Content::json(result)?]))
    }
}
```

> `#[tool]` を付けたメソッドは自動でクライアントに公開され、引数型（`schemars::JsonSchema` 派生）から
> 入力スキーマが生成・提示される（ジュニアがスキーマを手書きする必要はない）。

**DoD（T1）:** `model.load`→`model.query`→`result.get` がスキーマ提示つきで動作。

---

## 4. T2: 書込系ツール（単一ライタ・破壊的操作の識別）

設計書 §13 / 付録A R29。

- **編集は P3 §3.3 の `EditCommand` を生成して `UndoStack::run` に流す**（GUI と同一経路＝単一ライタ）。
- **書込系（編集・保存・上書き）は破壊的操作を識別子付きで返し**、クライアントに確認させる前提の
  レスポンスにする。

```rust
// squid-n-mcp/src/tools_write.rs
// model.edit : 部材追加・断面変更・荷重設定 → EditCommand 化して適用（Undo 可能）
// model.save : 上書き保存（.scz）。破壊的なので操作IDを返す

#[derive(schemars::JsonSchema, serde::Serialize)]
pub struct WriteResult {
    pub op_id: String,        // 破壊的操作の識別子（クライアント確認・Undo 参照用）
    pub undoable: bool,
    pub summary: String,      // 何が変わるか（確認用）
}
```

> **設計意図:** GUI（P3）・MCP（P8）の編集が**同じ `EditCommand`＋単一 `UndoStack`** を通ることで、
> モデルの一貫性（単一ライタ）が保たれ、Undo/Redo・直列化が共通になる。

**DoD（T2）:** MCP からの編集が Undo 可能。書込系が `op_id` を返す。GUI と MCP が競合せず直列に適用。

---

## 5. T3: 非同期ジョブ（解析の投入＋進捗）

設計書 §13。**解析は時間がかかるため、ジョブ投入＋進捗（notification/resource）の非同期パターン**。

```rust
// squid-n-mcp/src/jobs.rs
pub type JobId = String;
#[derive(Clone, serde::Serialize, schemars::JsonSchema)]
pub enum JobStatus { Queued, Running { progress: f32 }, Done { result_ref: String }, Failed { error: String } }

pub struct Job { pub id: JobId, pub kind: JobKind, pub status: JobStatus }
pub enum JobKind { LinearStatic, Eigen, Pushover, TimeHistory, DesignCheck }
pub struct JobRegistry { /* JobId -> Job。進捗更新で notification 送出 */ }

// ツール:
//   analysis.run   : 解析種別＋パラメータを受け、Job を作って **即 JobId を返す**（非同期）。
//                    実計算は tokio タスクで実行（P2/P5/P6）。進捗は notification/resource で通知。
//   analysis.status: JobId の状態取得（ポーリング用）
//   result.get     : Done の result_ref から結果取得（P2 §6 の部分読出し）
```

- 実計算は**別タスク**で走らせ、サーバの応答性を保つ（編集の単一ライタとは分離。読み取り中心）。
- 進捗は **MCP notification**（または resource 更新）でクライアントへ。長時間はチェックポイント（P6 §5）と連携可。
- 決定性：解析本体は単一スレッド設定でビット一致（P0〜P6 を踏襲）。ジョブ管理の並行性は結果値に影響させない。

> **実装状況（2026-07 時点、`crates/squid-n-mcp/src/lib.rs`）:** `analysis_run` は
> ①ジョブ登録＋`Running{progress: 0.0}` へ遷移 → ②ロック解放後に `state.model` の複製を
> `tokio::task::spawn_blocking` へ渡して解析実行 → ③その完了待ちと `Job` の状態更新（`Done`/`Failed`）は
> 別の `tokio::spawn` タスクで行う、という形で実装済み（ツール呼び出し自体は `job_id` を即返す）。
> 現状は `JobKind::LinearStatic`（先頭荷重ケースの線形静的、`analyze_model`）のみが実処理を持ち、
> `result_ref` は結果（変位ベクトル）の JSON 文字列をそのまま保持する簡易実装（別ストアへの参照ではない）。
> 他ジョブ種別（`Eigen`/`Pushover`/`TimeHistory`/`DesignCheck`）・`result.get` ツール・MCP notification に
> よるプッシュ通知（進捗は現状 `analysis_status` のポーリングで代替）は未実装・将来対応。

**DoD（T3）:** `analysis.run` が即 `JobId` を返し、進捗通知後に `result.get` で結果取得。
線形静的・固有値・プッシュオーバー・時刻歴をジョブとして実行できる。

---

## 6. T4: design.check / report.export ツール

```rust
// design.check  : 指定範囲の断面算定（P3 許容応力度・P7 保有耐力）を実行し検定比を返す
// report.export : 結果サマリ生成（PDF/Excel/CSV。設計書 §12.5）。大物は非同期ジョブでも可
```

**DoD（T4）:** MCP から `design.check` で検定比、`report.export` でサマリが得られる。

---

## 7. T5/T6: ST-Bridge 入出力・GUI 拡充

### 7.1 ST-Bridge（squid-n-io）— 設計書 §12.5

- **日本の構造分野標準 ST-Bridge（XML, 2.0 系）**のインポート／エクスポート。
- **対象は幾何・断面・部材・荷重まで。完全往復は保証せず**（解析結果・独自属性は対象外）と**期待値を明記**。
- 内部モデルへの**変換層**を介す（正準モデルを唯一の真実に。外部形式を直接の解析対象にしない）。

```rust
// squid-n-io/src/stbridge.rs
#[derive(Debug, thiserror::Error)]
pub enum StbError {
    #[error("xml parse: {0}")] Parse(String),
    #[error("unsupported version: {0}")] Version(String),
    #[error("unmappable element: {0}")] Unmappable(String),
}
pub fn import_stbridge(xml: &str) -> Result<squid_n_core::model::Model, StbError> { todo!() } // 2.0 系
pub fn export_stbridge(model: &squid_n_core::model::Model) -> Result<String, StbError> { todo!() }
```

> **⚠ 要検証:** ST-Bridge 2.0 の要素・属性マッピングは**公式スキーマ**を一次資料に。
> 対応範囲（節点・部材・断面・荷重）と非対応（結果・独自属性）を README/ドキュメントに明記。

### 7.2 GUI 拡充（squid-n-app）— 設計書 §14

- P3 の最小GUI に、解析種別の網羅（固有値・プッシュオーバー・時刻歴の起動と結果ビュー）、
  ヒンジ状態・応力コンター・モード形・時刻歴グラフの可視化を追加。
- 解析の非同期実行は MCP のジョブ機構（§5）と整合させる（GUI からもジョブとして投げる）。

**DoD（T5/T6）:** ST-Bridge 代表モデルが import→export で意味的に保存（幾何・断面・部材・荷重）。
GUI から全解析種別を起動し結果を可視化。

---

## 8. T7: テスト・DoD（フェーズ全体）

### 8.1 MCP 一連動作（→ T0/T1/T2）★一部目視（Inspector）
- 【目視/手動】MCP Inspector でモデル読込→解析→結果取得が一連で動作。スキーマが正しく提示。
- 【自動】各ツールのハンドラを直接呼ぶ統合テスト（rmcp 越しでなく関数として）で入出力を検証。
  編集は `EditCommand` 経由で Undo 可能（P3 §3.3 のテストを MCP 経路でも）。

### 8.2 非同期ジョブ（→ T3）
- 【自動】`analysis.run`→`JobId`→（進捗）→`result.get` の流れがテストで通る。Failed 時のエラー伝播。

### 8.3 ST-Bridge（→ T5）
- 【自動】代表モデルの import→export→再import で幾何・断面・部材・荷重が**意味的に一致**
  （完全バイト一致ではなく、対象範囲の意味保存）。

### 8.4 フェーズ DoD チェックリスト
| # | ゴール（§0.2） | 判定 | 章 |
|---|---|---|---|
| 1 | MCP 読込→解析→結果 一連動作・スキーマ提示 | §8.1 | §2,§3 |
| 2 | 書込系 単一ライタ・破壊的操作ID・Undo | §8.1 | §4 |
| 3 | 非同期ジョブ 投入→進捗→結果 | §8.2 | §5 |
| 4 | ST-Bridge 意味的往復 | §8.3 | §7.1 |

> **全チェック緑＝ P8 完了。** 次は P9（仕上げ：V&V・配布）で v1.0 完結。

---

## 9. 補足（ジュニア向け）

### 9.1 MCP とは（→ §2）
MCP（Model Context Protocol）は、AI エージェント等のクライアントが「ツール（関数）」「リソース」を
標準プロトコルで呼ぶ仕組み。rmcp は公式 Rust SDK。`#[tool]` を付けた関数が外部に公開され、入出力
スキーマ（schemars）がクライアントに提示される。トランスポートは stdio（ローカル）／HTTP（リモート）。

### 9.2 なぜ非同期ジョブか（→ §5）
解析は数秒〜数分かかる。同期ツールで待たせるとクライアントがブロックし、進捗も分からない。そこで
`analysis.run` は**即ジョブIDを返し**、実計算は裏で走らせ、進捗を notification で送る。クライアントは
完了通知後に結果を取りに来る。長時間ジョブはチェックポイント（P6）で中断・再開できる。

### 9.3 単一ライタ（→ §4）
モデルを書き換える経路（GUI・MCP）が複数あると競合する。本システムは**編集を1つの `EditCommand`＋
`UndoStack` に集約**し、直列に適用する（単一ライタ。R29）。読み取り（query/result）は並行してよい。

---

## 10. 用語（P8 で増えるもの）
| 用語 | 意味 |
|---|---|
| MCP | Model Context Protocol。ツール／リソースの標準プロトコル |
| rmcp | MCP の公式 Rust SDK（1.x） |
| ツール | MCP で公開する関数（model.edit, analysis.run 等） |
| 非同期ジョブ | 投入で即IDを返し裏で実行、進捗を通知する方式 |
| 破壊的操作識別子 | 書込系が返す op_id。確認・Undo 参照に使う |
| 単一ライタ | モデル編集を1経路に直列化する方針（R29） |
| schemars | Rust 型から JSON スキーマを生成（型安全な提示） |
| ST-Bridge | 日本の構造分野のデータ交換標準（XML） |
| 意味的往復 | バイト一致でなく対象範囲の意味が保存される往復 |

---

*本仕様書は P8（操作・連携：MCP・ST-Bridge・GUI拡充）を対象とする。IFC・認証・結果往復は将来。*
*rmcp の API は §2 で 1.7 系を具体化済み（`crates/squid-n-mcp/src/lib.rs` の実装と一致、2026-07 訂正）。*
*ST-Bridge のみ公式スキーマ（XML 仕様＝外部データ）に*
*沿ったマッピング作業が残る（API 調査ではなくデータ仕様の写経）。次は P9（仕上げ）で v1.0 完結。*
