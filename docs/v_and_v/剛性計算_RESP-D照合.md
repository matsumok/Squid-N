# 剛性計算の RESP-D マニュアル照合レポート

**照合日:** 2026-07-10
**原典:** RESP-D 操作・計算マニュアル 計算編「02. 剛性計算」
**方法:** マニュアル全節と実装（squid-n-core / squid-n-section / squid-n-element /
squid-n-design-jp / squid-n-app）を突き合わせ、差異を「修正済み」「既知の未実装
（仕様に明記あり）」「未実装（明記なし・要ロードマップ化）」に分類した。

---

## 1. 今回修正した差異

### 1.1 RC/SRC 断面の as_y/as_z に鉄筋断面積が入っていた（重大バグ）

- **箇所:** `squid-n-core/src/section_shape.rs` `to_section()`
- **マニュアル:** RC のせん断変形用断面積は `As = B·D/κ`（κ=1.2）。
- **不具合:** `Section.as_y/as_z` は全消費側（ビーム要素の φ 項・せん断ばね・
  鋼材検定・UI 表示）で「せん断断面積」の意味なのに、RC/SRC の生成側だけが
  主筋総断面積を格納していた。RC 柱梁のせん断剛性が約 2 桁過小評価される。
- **修正:** RC 矩形/円形は `A/1.2`、SRC は `rcAs + sAs·(ngs−1)`（ngs は暫定的に
  N_S_EQ で代用）を格納。壁（RcWall）も `A/1.2`。

### 1.2 鋼断面パラメトリック生成の as_y/as_z が未設定（5A/6 フォールバック）

- **マニュアル:** S 造は `As = Aw/κ`（κ=1.0、Aw=ウェブ断面積）。
- **不具合:** `SteelH` 等は as=0 のまま生成され、要素側で矩形近似 `5A/6` に
  フォールバック → H 形で約 2 倍のせん断剛性過大。
- **修正:** 強軸側（as_z↔Iy）=ウェブ（`tw·H`、設計検定側 `steel::shear_area`
  と同一規約）、弱軸側（as_y↔Iz）=フランジ。角形鋼管は 2 面の板、円形鋼管は
  `A/2`。CFT は鋼管部のみ（コンクリート無視の既存方針を踏襲）。

### 1.3 SRC の軸剛性用断面積に鉄骨等価分が未累加

- **マニュアル:** `An = rcAn + sAn·(ns−1)`。
- **不具合:** `Section.area` が質量算定と兼用のため、コンクリート全断面のみと
  していた（コード内に意図の明記あり）。軸剛性 EA が大幅過小。
- **修正:** `SectionShape::calc_axial_stiffness_area()` を新設し、
  `BeamElement` は剛性用 `a`（SRC は等価換算累加）と質量用 `a_mass`
  （幾何断面）を分離。質量への影響なし。

### 1.4 RC/SRC 矩形ねじり定数のアスペクト比 10 以上での打ち切り

- **マニュアル:** `J = (B³D/16)[16/3 − 3.36(B/D)(1 − (1/12)(B/D)⁴)]`（全域適用）。
- **不具合:** 実装は r=h/b≥10 で β=1/3 に切替え、r=10 で約 +6.7% の乖離
  （r<10 では別系近似で差 ±0.12% 以内）。
- **修正:** マニュアル式をそのまま全域に適用（`rect_torsion_j()`）。

### 1.5 剛域自動算定が構造種別を無視（S 造にも D/4 ルールを適用）

- **マニュアル:** S 造・CFT は剛域長 0。混合構造は非対称ルール
  （RC/SRC 部材は RC/SRC 直交材の最大せいフェイスから D/4 内側、
  S/CFT 部材は RC/SRC 直交材のフェイス**まで**、直交材がすべて S なら 0）。
- **不具合:** `auto_rigid_zones` が全梁要素に一律 `λ = D_orth/2 − D_self/4` を
  適用。S 造ラーメンに本来 0 の剛域が入り剛性過大。また直交せい探索に
  壁・シェル等の要素種別も混入し得た（「耐震壁周辺の剛域は考慮しない」に反する）。
- **修正:** `Section.shape`（無ければ Material.fc/fy）による種別判定を導入し、
  上記ルールを実装。直交せい探索は Beam 要素のみに限定。
  種別不明（shape なし・fc/fy なし）は従来挙動（RC 扱い）を維持。

### 1.6 PanelZone::new の dc/db 取り違え

- **不具合:** `evaluate()` は dc=柱せい・db=梁せいの前提（テスト・数値例照合
  と一致）だが、`new()` は水平材（梁）の depth を dc に、鉛直材（柱）を db に
  代入していた。現状 PanelZone 要素の生成経路が無く実害は未発現だが、
  将来接続した時点で全結果が誤る。
- **修正:** 対応を入れ替え、回帰テストを追加。

### 1.7 カタログの as_y/as_z doc コメント（表記のみ）

- CSV 実データ・要素側ペアリング（as_z↔Iy）と逆の「強軸/弱軸」表記だったため
  訂正。数値への影響なし。

---

## 2. 既知の未実装（仕様・コードに明記あり、今回は対象外）

| 項目 | マニュアル該当 | 明記箇所 |
|---|---|---|
| 耐震壁の壁エレメント実剛性（κ 式・開口低減 r=1−1.25√(h0l0/hl)・上下梁100倍） | 耐震壁(壁エレメントモデル) | specs/P5.5_壁とMS.md（TVLEM 採用予定・T0 文献確定待ち）、docs/v_and_v/pending_items.md |
| RC 断面の鉄筋考慮 (n−1)Σai（An・Ie） | RC大梁/RC柱の断面性能 | specs/UI設計.md §4.2「弾性剛性は原則 gross」 |
| ハンチの剛域（25°/60° ルール） | 剛域の計算 2. | specs/構造計算一貫プログラム_実装設計書.md（一様断面扱い・将来オプション） |
| CFT の充填コンクリート剛性（A/I/J とも） | S柱 CFT | section_shape.rs doc コメント |
| SRC/CFT ねじりの複合換算 J=(sG/cG)sJ+cJ | SRC/CFT | section_shape.rs コメント（Material 依存のため保留） |
| SRC ヤング係数比 ns の材料からの算定（現状 N_S_EQ=15 固定） | SRC 断面性能 | section_shape.rs doc コメント |
| パネルと剛域の併用（はみ出し分を剛域化） | 仕口パネルの計算 | specs/P1_線形要素.md（方式 A/B の二者択一で回避） |

## 3. 未実装（スコープ外の明記なし・要ロードマップ化）

| 項目 | マニュアル該当 | 影響 |
|---|---|---|
| スラブ協力幅による剛性増大率（RC 規準 8 条、連続梁 λ、吹抜け補正） | RC大梁 剛性増大率 | 床付き RC 架構の梁剛性過小。現状は増大済み断面値の手動入力で代替 |
| 腰壁・垂壁・袖壁の断面性能への考慮／それに伴う剛域入り長さ | RC大梁/RC柱・剛域 3. | 雑壁付き部材の剛性過小 |
| ブレース剛性 KB=EA/L（専用要素）・引張専用の弾性 1/2 則 | ブレースの剛性 | ブレースが曲げ剛性を持つ梁要素として解析される |
| RC 耐震壁の判定（壁厚≥120mm・開口周比 r0≤0.4）と等価開口置換 | 耐震壁 | 壁モデル化の前提条件チェックが無い |
| フレーム内雑壁の周辺部材への算入 | その他 雑壁の剛性 | 同上 |
| S 造 BOX 柱パネルのウェブ厚 2 倍・CFT パネルの等価コンクリート置換 | 仕口パネルの計算 | 設計検定側（joint_wiring）は板厚 1 枚分で評価（安全側だが規定と相違） |
| RC/SRC パネル寸法の「B−2dt（主筋間距離）」（現状は RC 規準 15 条 bj 方式を意図的採用） | 仕口パネルの計算 | 設計判断として joint.rs に明記あり。RESP-D 準拠を要する場合は切替オプションが必要 |
| PanelZone 要素の自動生成経路（現状 factory 消費のみで生成側なし） | 仕口パネルの計算 | パネル剛性が解析に反映されない（剛域近似 方式 A のみ） |
| 間柱ダンパー・免震支承材・製品ブレース（アンボンド/KT/二重鋼管等）の剛性 | 各節 | 該当要素種別が未実装（製品対応はロードマップ範囲） |

---

## 4. 検証

- `cargo test --workspace` 全緑（修正断面性能・剛域・パネルの回帰テストを追加）
- `cargo clippy --workspace --all-targets --locked -- -D warnings` 警告なし
- RC 矩形 J のマニュアル式一致・SRC 累加値・S ウェブせん断断面積は
  単体テストで数値照合済み
