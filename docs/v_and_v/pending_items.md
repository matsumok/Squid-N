# P9 未完了項目（仕様書と実装の乖離）

本ドキュメントは、P9 仕様書（specs/P9_仕上げ.md）と現在の実装との乖離を記録する。

## 凡例

| 評価 | 意味 |
|------|------|
| ✅ | 仕様を満たす実装が存在 |
| 🔶 | 一部実装 |
| ❌ | 未実装 |

---

## 1. プッシュオーバー検証（V&V）

**仕様要求（P9 §3）:**
- 1層ラーメンの終局機構・崩壊荷重（P5 §10.1）
- 弧長法による軟化追跡
- ヒンジ発生順序・崩壊機構の自動判定

**現状: 🔶 一部実装**
- `pushover.rs`: 線形静的1ケースの結果を返す stub
- 非線形増分・弧長法・変位制御は未実装（P5 の完了が必要）

**対応方針:** P5 フェーズで非線形解析が完了後、本格実装と V&V を実施。

---

## 2. 壁（壁谷澤 TVLEM）検証（V&V）

**仕様要求（P9 §3）:**
- Three Vertical Line Element Model (TVLEM)
- 既往実験／商用ソフトに対し初期剛性・降伏点・最大耐力・履歴形状が許容差内

**現状: ❌ 未実装**
- TVLEM 構造体なし
- `ElementKind::Wall` は factory.rs で BeamElement に fallback
- P5.5（壁とMS要素）の完了が前提

**対応方針:** P5.5 フェーズで壁要素を本実装後、実験照合 V&V を実施。
P9 §0.3 のスコープ境界にも「壁の V&V は P5.5 完了後」と明記あり。

---

## 3. 時刻歴応答解析（決定性テスト）

**仕様要求（P9 §4）:**
- 全解析種別で CPU 単一スレッドのビット一致を検証

**現状: 🔶 一部実装**
- 設定構造体（NewmarkCfg, HhtCfg, RayleighDamping）のみ実装
- 時刻歴ソルバ本体は P6 で実装予定

**対応方針:** P6 フェーズで時刻歴ソルバ実装後、決定性テストを追加。

---

## 4. 性能ベンチマーク（T2）

**仕様要求（P9 §4）:**
- criterion による線形静的・固有値・プッシュオーバー・時刻歴の各1ステップ計測
- CI で規定%以上の劣化でビルド失敗

**現状: ❌ 未実装**
- criterion の依存なし
- benches/ ディレクトリなし
- 基準モデル・基準機の定義は未着手

**対応方針:**
- criterion を [dev-dependencies] に追加
- 各クレートに benches/ を作成
- 技術リード提供の基準モデル到着後、CI 閾値を設定

---

## 5. UI-16 レポート出力（PDF/Excel/CSV）

**仕様要求（UI設計 §8 / §9.3）:**
- PDF 計算書出力
- Excel（xlsx）表出力
- CSV 出力
- 章選択・図表

**現状: ❌ 未実装**
- sc-app は pub fn placeholder() {} のみ
- GUI フレームワークの選定・実装は未着手

**対応方針:** GUI アプリは P3 で土台実装後、P9 でレポート出力機能を追加。

---

## 6. 準拠規準版の明記

**仕様要求（P9 §5.1）:**
- 準拠した AIJ 規準・告示の版を明記

**現状: 🔶 一部**
- README.md に記載場所を追記
- 具体的な規準版の一覧は技術リードの確定待ち（R4/R23）

---

## 7. P3（最小UI/設計）未完了項目

**仕様要求（P3 §0.2 / §8.5）:**
- 表入力で節点・部材・断面・荷重を作成・編集（§3）
- 編集トランザクション（Undo/Redo・MCP 共通）（§3.3）
- GUI から線形静的・固有値・地震（Ai一気通貫）を実行し結果閲覧（§4）
- 3D ビューア（形状・変形・モード形）・N/Q/M 図・CMQ 図（§5）
- 許容応力度（RC/S 一次設計）の検定比（§6）
- 検定比 色分け一覧表（§7）

**現状（2026-06 監査後・rustc 1.96 昇格後・P3 残項目実装後）:**

| 項目 | 状態 | 備考 |
|------|------|------|
| 許容応力度の数値（鋼梁曲げ §6.4） | ✅ | 単位バグ（M*1000）を修正、検定比=0.1197 に厳密一致するテストを追加 |
| RC 許容応力度（Fc 参照・鉄筋グレード・短期上限） | ✅ | `Material.fc` を新設し Fc を正しく取得。鉄筋グレードを `mat.name` から。SD345 短期上限を F=345 ベースに |
| RC 曲げの本格検定式（a_t·j 等） | 🔶 | `Section` に引張鉄筋断面積 a_t が無いため暫定（σc=M/Z でコンクリート圧縮検定）。P4 で SectionShape 経路が整ってから本格実装 |
| RC せん断検定 | 🔶 | τ=Q/(b·j), j=7d/8 の暫定式を実装。αs·fw 等の詳細式は P7/AIJ 外部データ |
| 編集トランザクション（Undo/Redo） | ✅ | `sc-edit` クレートに統一。断面・荷重編集コマンド（SetSectionField/SetSectionName/SetElementSection/SetLoadCaseName/SetNodalLoad/DeleteNodalLoad）も追加 |
| GUI feature のビルド・検証 | ✅ | rustc 1.96 に昇格し egui 0.34.3 がビルド可能。`cargo build/clippy/test --features gui` が通る。`eframe::App::ui` メソッドに移行 |
| T0: sc-app 雛形（eframe 起動・タブ） | ✅ | `main.rs` + `[[bin]] structcalc` を追加。タブ切替・ツールバー実装 |
| T1: テーブル入力（節点座標編集可能） | ✅ | 節点テーブルの X/Y/Z を `TextEdit` で編集可能。`SetNodeCoord` 経由で Undo/Redo 対応。数値以外は赤背景 |
| T1: テーブル入力（部材・断面・荷重） | ✅ | 部材: 断面割当を ComboBox で編集（`SetElementSection`）。断面: 名称・A・Iy・Iz・J を TextEdit で編集。荷重: ケース名・節点荷重 6 成分を TextEdit で編集。全て Undo/Redo 対応・数値赤背景 |
| T2: Undo/Redo GUI ボタン | ✅ | ツールバーに配置。`sc_edit::UndoStack` に接続 |
| T3: 解析実行（線形静的・固有値・地震） | ✅ | `run_linear_static`/`run_eigen`/`run_seismic` を実装。P2 `Analysis` API に接続 |
| T4: 3Dビューア（形状・変形・モード形） | ✅ | egui Painter で簡易 3D 描画（等角投影・ドラッグ回転/パン・ズーム）。形状・変形図・モード形（倍率スライダー・モード選択）対応。wgpu 本格統合は後続 |
| T5: 応力図 N/Q/M・CMQ図 | ✅ | N/Q/M 図: member_forces の評価位置を部材ローカルに沿ってポリゴン描画。CMQ 図: BeamLoad の c_i/c_j（モーメント・青）と q_i/q_j（せん断・橙）を描画 |
| T7: 検定比 checks 生成ドライバ | ✅ | `run_design_check` を実装。member_forces の全評価位置で `DesignCheck` を呼び検定比表にデータを供給 |
| T7: 検定比 色分け一覧表 | ✅ | `design_view.rs` で色分け（≤0.8 緑/0.8-1.0 黄/>1.0 赤）・内訳・根拠表示 |

**残項目（P3 スコープ外または後続依存）:**
- RC 曲げ・せん断の本格検定式 → P4 で SectionShape 経路が整ってから
- 3Dビューアの wgpu 本格統合（高品質描画・ピック） → 後続フェーズ
- 詳細は `docs/v_and_v/p3_review.md` の推奨対応を参照。

---

## 8. P4（材料・断面）未完了項目

**仕様要求（P4 §0.2 / §8.4）:**
- 各履歴則が規定の繰り返し変位履歴に対し参照ループと一致（`insta` スナップショット。§8.1）
- ファイバ断面の積分が、単軸引張・圧縮・純曲げで M–φ 理論値に一致（弾性域は厳密 1e-9。§8.2）
- 代表 RC 梁・柱で、スケルトンの折れ点（ひび割れ／降伏／終局）が手計算（規準式）と一致（§8.3）

**現状（2026-06 監査 → 監査後修正実装 → 残項目完了）:**

| 項目 | 状態 | 備考 |
|------|------|------|
| T0 `UniaxialMaterial` トレイト（trial/commit/revert） | ✅ | `clone_box` を追加（ファイバごとの状態複製用） |
| T1 コンクリート包絡線・ひび割れ・テンションスティフニング | ✅ | 圧縮軟化の向き・接線符号・ecu 境界連続性を修正。ひび割れを ε_cr で判定。テンションスティフニング検証テスト追加。`residual_ratio` パラメータ化 |
| T2 バイリニア・MP | ✅ | バイリニアを kinematic hardening に改めバウシンジャー検証追加。MP の反転検知を trial 内へ移行し ξ 更新を標準式化・バウシンガーループテスト追加。insta スナップショット（`tests/uniaxial_snapshots.rs`）追加 |
| T3 武田・原点指向・スリップ履歴則 | ✅ | `HysteresisMaterial` で `UniaxialMaterial`(trial/commit/revert) を実装。insta スナップショットでループ固定。武田内側ルール（ポリゴン則）・TakedaDegrading（ピーク到達回数に基づく耐力劣化）を本格実装 |
| T4 ファイバ断面（弾性厳密・降伏進展） | ✅ | ファイバごとに独立状態（`mats.len()==fibers.len()`）。弾性域厳密(1e-9)・降伏進展テスト追加 |
| T5 スケルトン自動算定（折点手計算一致・軸力依存） | ✅ | `build_rc_member_skeleton`: 鉄筋点ファイバ組込み・ひずみイベント（ひび割れ/降伏/ピーク）で折点抽出・塑性ヒンジ M-θ 変換・せん断変形/鉄筋抜出し加算（§7 フロー4）・手計算 My=at·σy·j・Mu=0.9·at·σy·j 照合テスト追加。軸力依存テスト追加 |
| T6 テスト・DoD（§8.1/§8.2/§8.3） | ✅ | insta スナップショット 6件（武田/原点指向/スリップ + Concrete/Bilinear/MP）。ファイバ弾性厳密・降伏進展。スケルトン手計算一致（My・Mu）。`cargo fmt --check`・`clippy --all-targets --locked -D warnings`・`cargo test --workspace` 全て緑 |

**P4 DoD §8.4 達成状況:**
| # | ゴール（§0.2） | 判定 | 章 |
|---|---|---|---|
| 1 | 履歴則 参照ループ一致 | ✅ | §3,§4,§5 |
| 2 | ファイバ M–φ 理論一致（弾性厳密） | ✅ | §6 |
| 3 | スケルトン折れ点 手計算一致 | ✅ | §7 |

**残項目（P4 スコープ外・後続フェーズ）:**
- RC 曲げ Mu の精度向上（圧縮鉄筋・SE断面・付着すべりの本格モデル）→ P5/P7
- 履歴則の実験データとの照合（Validation）→ P9
- 詳細は `docs/v_and_v/p4_review.md` を参照。

---

## 経過

| フェーズ | 依存する P9 項目 | 状態 |
|----------|-----------------|------|
| P3 (最小UI) | 許容応力度・表入力・解析起動・checks・3Dビューア・応力図 ✅ | ✅ |
| P4 (材料・断面) | 履歴則スナップショット・ファイバ M-φ・スケルトン手計算 ✅ | ✅ |
| P5 (非線形) | プッシュオーバー V&V | 🔶 |
| P5.5 (壁/MS) | 壁 TVLEM V&V | ❌ |
| P6 (動的) | 時刻歴 V&V・決定性 | ❌ |

P9 の完了には上記フェーズの完了が前提となる。
