# 4.2 トラス（ブレース）要素

軸剛性のみをもつブレースを \\( K_B = E \cdot A/L \\) で算定する（トラス要素の力学）。

**算定式**：\\( k_a = \text{factor} \cdot E \cdot A/L \\) とする。
引張専用ブレースは弾性解析で剛性を 1/2、弾塑性解析で初期剛性を 1 倍とし、圧縮スラック側は残留剛性比 1e-6 とする。

**実装**：`frame::truss`（`truss.rs`）が算定する。

**整合性**：引張専用ブレースの弾性 1/2 化・弾塑性スラック側の残留剛性比は実装 doc に明記している。
