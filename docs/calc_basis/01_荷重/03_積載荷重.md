# 1.3 積載荷重

## 1.3.1 柱の積載荷重低減（支持床数による低減）

柱の積載荷重は、建築基準法施行令85条2項に基づき、支える床の数に応じて柱および基礎の積載荷重を低減する。

**算定式（低減率表）**：支持床数 1→1.0、2→0.95、3→0.90、4→0.85、5→0.80、6→0.75、7→0.70、8→0.65、9以上→0.60。

**実装**：`squid_n_load::live_load::column_live_load_reduction`（`live_load.rs:30`）が算定し、支持床数は `floors_supported_by_column` が求める。
`Model.load_cfg.live_load_reduction`（既定 false。既定では低減しない）が有効なときのみ適用する想定である。

**整合性**：doc に「令85条2項」を明記しており、低減率表は令85条の表と一致する。
なお令85条2項ただし書（劇場・映画館・演芸場・観覧場・公会堂・集会場等の客席・集会室など、床の用途によっては低減できない）の用途判定は未実装であり、該当用途では低減を適用しないよう利用者が設定する必要がある。
