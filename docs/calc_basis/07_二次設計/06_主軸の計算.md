# 7.6 主軸の計算

地震力方向に対する架構の主軸を、水平力のなす仕事の極値条件から計算する（実務的手法）。

**算定式**

\\[ \tan 2\Theta = -\frac{P^t(u_y+v_x)}{P^t(v_y-u_x)} \\]

仕事 W の極値から主軸角 Θ を決定する。

**実装**：`secondary::principal_axis`（`principal_axis.rs`）が計算する。

**整合性**：仕事 W の極値条件からの導出を実装 doc に明記している。
