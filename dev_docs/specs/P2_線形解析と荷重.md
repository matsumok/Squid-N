# P2 線形解析＋荷重 実装仕様書

**対象フェーズ:** v0.5 プロトタイプ / P2 線形解析＋荷重（設計書 §18）
**対象読者:** Rust ジュニアエンジニア（解析・荷重の理論は §8 の補足で補う）
**親文書:** `../構造計算一貫プログラム_実装設計書.md`（以降「設計書」。`§x.y` はその章番号）
**先行フェーズ:** [P0](P0_基盤.md) / [P1](P1_線形要素.md) /（板を使う場合）[P1.5](P1.5_板要素.md)
**前提環境:** Rust stable 1.8x 以降 / `faer = "0.24"` / `arrow`・`parquet`

---

## 0. このフェーズについて

### 0.1 目的

**単一モデル**の上で、線形静的解析と振動（固有値）解析が**シームレスに走り**、日本の地震荷重
（Ai分布）と床荷重を自動生成して解析へ流し込むところまで作る。設計書 §9 / §10.1–§10.4 / §12.3。

- **Analysis API**：複数荷重ケース・組合せの線形静的を、分解1回・後退代入多数で高速処理（§10.2）。
- **固有値解析** `Kφ=λMφ`：**部分空間反復法**（下位モード）。分解は **P0 の SPD Cholesky を再利用**。
- **squid-n-load**：床面荷重→部材荷重の自動分割（小梁考慮・CMQ図）、Ai分布（略算＋準精算）。
- **一気通貫**：固有値→実固有周期 T→Ai分布→静的解析への水平力投入を自動接続（設計書 §9.3 の核心）。
- **結果I/O**：Parquet で結果を列指向格納（ストリーミング書込・部分読出し）。

### 0.2 完了像（ゴール）

設計書 §18 P2 完了基準「**単一モデルで静的＋振動／告示Ai一致**」を次で判定:

1. **線形静的**：単純梁・ラーメンで変位・応力が手計算と一致（厳密、相対 1e-9）。複数荷重ケース・
   組合せが1回の分解で解ける（§7.1）。
2. **固有値**：等価せん断モデルの固有周期が理論値に**収束**（反復法なので許容差判定）。有効質量比
   合計が（十分なモード数で）≈100%（§7.2）。
3. **告示 Ai**：Ai 式の自己整合（計算例）と、**出典告示の計算例**との Ci・Qi 一致（§7.4）。
4. **CMQ・荷重保存**：三角／台形分配の総和が床総荷重に一致（厳密）、CMQ（固定端モーメント・せん断）が
   理論値と一致（§7.3）。
5. **一気通貫**：固有値由来 T で Ai が変わり、その水平力で静的解析が走る（§7.5）。
6. **結果I/O**：静的・モーダル結果を Parquet にストリーミング書込し、特定ケース・部材を部分読出し（§7.6）。
7. 決定性：同一入力・単一スレッドでビット一致（P0/P1 踏襲）。**ただし反復解法（固有値）の
   「理論への一致」は許容差判定**で、「実行間ビット一致」とは別概念（§7.2 の注意）。

### 0.3 スコープ境界（含む／含まない）

| 項目 | P2 で | 備考 |
|---|---|---|
| Analysis API（複数ケース・組合せ 線形静的） | **含む** | §10.2 |
| 質量行列（集中／整合） | **含む** | §6.1/§10.4。P1 のスタブを実装 |
| 固有値 `Kφ=λMφ`（**部分空間反復法**） | **含む** | §10.4。下位モード |
| **shift-invert Lanczos／内部モード／疎LDLT** | **含まない → 将来** | §0.4 参照 |
| 床荷重分割（スラブ→小梁→大梁・CMQ） | **含む** | §9.1 |
| Ai分布 略算（Ci=Z·Rt·Ai·C0） | **含む** | §9.2 |
| Ai分布 **準精算**（固有値 T を Ai式へ代入） | **含む** | §9.3 |
| Ai分布 **精算（SRSS/CQC モード重ね合わせ）** | **含まない → 将来** | §9.3(b)。下記注意 |
| 一気通貫（固有値→Ai→静的） | **含む** | §9.3 |
| 結果 Parquet I/O（ストリーミング・部分読出し） | **含む（P2 規模）** | §12.3。下記注意 |
| 時刻歴・1GB級結果の高速部分読出し | **含まない → P6** | §12.6 のスケールは時刻歴用 |
| 断面算定（許容応力度） | **含まない → P3** | |

### 0.4 重要な技術決定（書き始める前に確認した事項）

- **固有値は部分空間反復法（subspace iteration）を採用。** 理由：用途は「下位〜30モード」（§1.3）で、
  この解法は **K 単独を分解**（`K X_{k+1} = M X_k` を反復）するため **K は SPD のまま＝P0 の Cholesky を
  そのまま使える**。一方 **shift-invert Lanczos は `(K − σM)` を分解するが、これは対称*不定*** になり、
  P0 が見送った LDLT/Bunch-Kaufman が必要になる。内部モードが要るまで shift-invert は**見送る**。
- **Ai 精算は「準精算」まで。** SRSS/CQC のモード重ね合わせは、刺激係数・組合せ規則を早期に持ち込み、
  検証が時刻歴包絡（P6）に依存するため**見送る**（ユーザ確認済み）。
- **Parquet の DoD は P2 が実際に生む結果（静的＋モーダル＝小規模）に合わせる。** §12.6 の
  「1GB級→100ms部分読出し」は時刻歴（P6）のスケール。**機構（ストリーミング書込・部分読出し）は作るが、
  テストは P2 規模**で行う。

---

## 1. タスク一覧と依存順序

```
T0 質量行列（集中/整合）ElementBehavior::mass_matrix 実装  (§2)
   └─> T2 固有値 部分空間反復（Kφ=λMφ）(§4)
T1 Analysis API（複数ケース・組合せ 線形静的）(§3)
   └─> T4 一気通貫（固有値→Ai→静的）オーケストレーション (§5.3)
T3 squid-n-load 床荷重分割（小梁・CMQ）(§5.1)
T4 squid-n-load Ai分布（略算＋準精算）(§5.2) ─ depends T2,T3
T5 squid-n-io Parquet 結果I/O (§6)
T0..T5 ─> T6 テスト・DoD (§8)
```

| ID | タスク | クレート | 依存 |
|---|---|---|---|
| T0 | 質量行列（集中／整合） | squid-n-element | P1 |
| T1 | Analysis API（複数ケース・組合せ静的） | squid-n-solver | P1 §10 |
| T2 | 固有値 部分空間反復法 | squid-n-solver | T0, P0 ソルバ |
| T3 | 床荷重分割（スラブ→小梁→大梁・CMQ） | squid-n-load（新規） | P0 §4.7 |
| T4 | Ai分布（略算＋準精算）＋一気通貫 | squid-n-load / squid-n-solver | T2, T3, P0 §4.5 |
| T5 | 結果 Parquet I/O | squid-n-io | P1 結果型 |
| T6 | テスト・DoD | 全体 | 上記 |

---

## 2. T0: 質量行列（集中／整合）

設計書 §6.1（`mass_matrix`）/ §10.4。P1 で雛形だった `ElementBehavior::mass_matrix` を実装する。

**集中質量・整合質量の明示式（閉形式。文献転記不要）。** `m = ρ·A·L`（要素全質量）。

```
■ 集中質量（対角）: 並進 6 自由度（ux,uy,uz × 両端）に m/2 ずつ。回転 3×2 は 0。
    M_lumped = diag(m/2, m/2, m/2, 0,0,0,  m/2, m/2, m/2, 0,0,0)

■ 整合質量（曲げ。Bernoulli 整合質量。DOF 並びは §4.1 の梁と同じ）:
    曲げ面（例 uy,rz）の 4×4 ブロックは (ρAL/420)·
        [ 156    22L     54    −13L  ]
        [ 22L    4L²     13L   −3L²  ]
        [ 54     13L     156   −22L  ]
        [ −13L   −3L²    −22L   4L²  ]
    軸（ux_i,ux_j）: (ρAL/6)·[[2,1],[1,2]]
    ねじり（rx）: (ρ·J/A·L/6)·[[2,1],[1,2]]（極慣性。簡易には集中可）
    xz 面（uz,ry）も同じ 4×4（符号は §4.1 の曲げ規約に合わせる）
```

```rust
// squid-n-element：BeamElement / ShellElement に実装
impl ElementBehavior for BeamElement {
    fn mass_matrix(&self, opt: MassOption) -> LocalMat {
        let m = /* ρ */ self.density() * self.a * self.length; // 要素全質量
        let mut mm = LocalMat::zeros(12);
        match opt {
            MassOption::Lumped => {
                for d in [0,1,2,6,7,8] { mm.set(d, d, m/2.0); } // 並進のみ、回転 0
            }
            MassOption::Consistent => {
                // 上の (ρAL/420) 係数の 4×4 を uy-rz, uz-ry に、(ρAL/6) を軸・ねじりに配置
                // （DOF 添字は §4.1 と同一。各項を set で埋める）
            }
        }
        mm
    }
}
```

### 2.1 集中質量が M を特異にする点（★固有値に直結）

- 集中質量は**回転自由度の質量が 0**。よって全体 M は**特異**（質量ゼロの自由度を持つ）。
- **部分空間反復法では扱える**：`M·X_k` は質量ゼロ自由度の右辺成分が 0 になるだけで、分解する K は
  SPD のまま。ただし:
  - **意味のあるモード数は「非ゼロ質量自由度の数」で上限**が決まる。
  - Rayleigh–Ritz で **M ノルムがゼロの Ritz ベクトルをガード**する（質量ゼロ方向に偽モードを出さない）。
- 整合質量は SPD（特異でない）。集中質量で問題が出る場合の代替として用意（`MassOption`）。

**DoD（T0）:** 1要素の集中質量の総和（並進）が `ρ·A·L`（梁）/ `ρ·t·面積`（シェル）に一致。
整合質量が対称・正定値。集中質量行列が対角。

---

## 3. T1: Analysis API（複数ケース・組合せ 線形静的）

設計書 §10.1/§10.2。P1 の `linear_static_once`（1ケース検証ハーネス）を、**製品API**へ拡張する。

```rust
// squid-n-solver/src/analysis.rs
pub struct Analysis<'m> {
    model: &'m squid_n_core::model::Model,
    dofmap: squid_n_core::dof::DofMap,
    reducer: crate::constraint::Reducer,         // P1 §9
    solver: Box<dyn squid_n_math::solver::LinearSolver>,
    factorized: bool,                            // K を分解済みか
}

impl<'m> Analysis<'m> {
    /// モデルから DofMap・拘束縮約・K 組立・分解までを一度だけ行う。
    pub fn prepare(model: &'m squid_n_core::model::Model) -> Result<Self, SolveError> {
        // DofMap::build → assemble_global_k → Reducer::build → reduce_k → solver.factorize
        // ※固有値解析も行う場合は M も同じ Reducer で縮約しておく（§4.1 / 下記注意）。
        todo!()
    }

    /// 1荷重ケースを解く（分解は再利用＝後退代入のみ）。
    pub fn linear_static(&self, lc: LoadCaseId) -> Result<StaticResult, SolveError> { todo!() }

    /// 荷重組合せ（係数つき和）を解く。各ケース結果の線形結合でも、合成荷重を解いてもよい。
    pub fn linear_combination(&self, combo: &LoadCombination) -> Result<StaticResult, SolveError> { todo!() }
}
```

> **設計意図（P1 との違い）:** P1 の `linear_static_once` は毎回 prepare からやり直す検証用。
> P2 の `Analysis` は **`prepare` で K を1回分解 → 各ケース/組合せは後退代入のみ**（設計書 §5.2/§10.2）。
> 「線形静的＝P2」の本体はここ。`StaticResult` は P1 の `StaticOnce`（変位・部材応力）を踏襲。

**DoD（T1）:** 同一 K で 3 荷重ケースを解き、各々が手計算一致。`prepare` の分解が1回だけ呼ばれる
（後退代入は複数回）。組合せ結果＝各ケース結果の線形結合と一致。

---

## 4. T2: 固有値解析（部分空間反復法）

設計書 §10.4。一般化固有値問題 `K φ = λ M φ` の**下位 n モード**を解く。

> **アルゴリズムは下記 §4.1 に手順を明示済み（写経可）。** 収束証明・派生（シフト等）の詳細のみ
> **K.-J. Bathe, *Finite Element Procedures*** を参照。実装は §4.1 の手順＋§8.2 の固有周期で照合する。

### 4.1 アルゴリズム（部分空間反復法）

> **⚠ M も K と同じ縮約空間に置く（必須・正確性に直結）:** 固有値問題は **縮約後の自由度空間**で
> `K_red φ = λ M_red φ` として解く。`K_red = TᵀKT` と同様に **`M_red = TᵀMT`** を作ること。
> とくに**剛床ダイアフラム下では、`TᵀMT` がスレーブ節点の並進質量をマスター節点へ移す**働きをする。
> これを省くとモード質量・**有効質量比（DoD #2）が物理的に誤る**（単なるラベル違いではない）。
> 実装：M を組み立てる `assemble_global_m`（K と同型・集中/整合）を追加し、**K と同じ `Reducer` に通す**。
> `Reducer` には `reduce_m`（= `reduce_k` と同一の `TᵀXT` トリプレット変換。対称疎行列なら何でも可）を足す。

```
入力: K_red (SPD, 縮約済み), M_red (= TᵀMT, 縮約済み), モード数 n, 部分空間サイズ q (= min(2n, n+8) 程度)
0. K_red を分解（P0 の DirectSparseCholesky を再利用 ← K_red は SPD なのでそのまま）
   ※ M_red = TᵀMT を K と同じ Reducer で事前に構築しておく
1. 初期部分空間 X0 (active×q) を用意（ランダム or 質量集中自由度に単位ベクトル）
2. 反復 k=0,1,...:
   a. K Y = M X_k を解く（前進後退代入のみ。q 本の右辺）
   b. 縮約: K* = Yᵀ K Y,  M* = Yᵀ M Y     （q×q の小行列）
   c. 小さい一般化固有値問題 K* z = θ M* z を密ソルバ（faer）で解く（Rayleigh–Ritz）
   d. X_{k+1} = Y z       （Ritz ベクトル）
   e. 収束判定: 下位 n 個の θ の相対変化 < tol で停止
3. λ_i = θ_i, φ_i = X 列。固有周期 T_i = 2π/√λ_i
```

```rust
// squid-n-solver/src/eigen.rs
pub struct ModalResult {
    pub omega2: Vec<f64>,          // λ = ω²
    pub period: Vec<f64>,          // T = 2π/ω
    pub shapes: Vec<Vec<f64>>,     // モード形（active 次元 → 展開して全体へ）
    pub participation: Vec<[f64;3]>,// 刺激係数（X,Y,Z 方向）
    pub effective_mass: Vec<[f64;3]>,// 有効質量比
}

impl<'m> crate::analysis::Analysis<'m> {
    /// 下位 n モードを部分空間反復で解く。basis は初期/接線/割線（P2 は初期 K のみ）。
    pub fn eigen(&self, n_modes: usize) -> Result<ModalResult, SolveError> { todo!() }
}
```

### 4.2 刺激係数・有効質量（設計書 §9.3）

- 各モードの刺激係数 `βi = (φiᵀ M r) / (φiᵀ M φi)`（`r` は方向影響ベクトル）。
- 有効質量 `Meff,i = (φiᵀ M r)² / (φiᵀ M φi)`。全モード和は**全質量**（十分なモード数で ≈100%）。

### 4.3 DoD（T2）— ★「理論一致(許容差)」と「実行間ビット一致」を区別

- **理論一致（許容差）:** 等価せん断モデルの固有周期が理論値に**収束**（反復法なので相対 1e-8 程度）。
  - 1自由度: `T = 2π√(m/k)`。例 `k=1000 N/mm, m=1.0 N·s²/mm → T = 2π√(1/1000) = 0.198692 s`。
  - 2層等質量等剛性せん断モデル: `K=[[2k,-k],[-k,k]], M=mI`。
    `λ = (k/m)·(3∓√5)/2`。例 `k=1000, m=1`：`λ1=382.0, T1=0.32150 s`／`λ2=2618.0, T2=0.12280 s`。
    2モードで**有効質量比合計＝100%**（モード1≈91.1%, モード2≈8.9%）。
- **実行間ビット一致（決定性）:** 同一入力・単一スレッドで `eigen` を 100 回 → **同一結果**
  （初期部分空間を固定シードにする）。※ これは「理論への一致」とは別物（反復の収束結果が
  毎回同一バイト、という意味）。
- **縮約空間の確認:** 固有値は `K_red φ = λ M_red φ` を解いていること。**M が K と同じ `Reducer`
  を通っている**こと（剛床ありモデルで、剛床なし等価モデルと固有周期・有効質量比が一致するかで検証）。

---

## 5. T3/T4: squid-n-load（荷重の自動生成）

新規クレート `squid-n-load`（`squid-n-core` のみに依存。Ai精算で必要な T は**呼び出し側＝squid-n-solver が渡す**ので
`squid-n-load` は `squid-n-solver` に依存しない＝layering 維持）。

```
squid-n-load/src/
├── lib.rs
├── floor.rs   # 床荷重分割・CMQ（§5.1）
├── ai.rs      # Ai分布 略算/準精算（§5.2）
└── combo.rs   # 荷重組合せ自動生成（§5.4）
```

### 5.1 床荷重分割（floor.rs）— 設計書 §9.1

解析モデルは柱・大梁まで（小梁は剛性に入れない）。スラブ（P0 §4.7 の `Slab`：範囲＋小梁配置）を
使って面荷重を大梁へ流す。**2段階**:

1. 面荷重 → 小梁へ（小梁が無い領域は直接大梁へ）。
2. 小梁反力 → 支持する大梁へ（集中／分布荷重）。

分配方式（スラブごと `DistributionMethod`）:
- **三角形・台形（45°）**：二方向スラブ。短辺方向は三角形、長辺方向は台形。
- **一方向**：一方向スラブ。
- **負担面積法**：面積按分。

```rust
// squid-n-load/src/floor.rs
/// 大梁要素への荷重（等分布・台形・集中）と、検証用 CMQ。
pub struct BeamLoad {
    pub elem: squid_n_core::ids::ElemId,
    pub shape: LoadShape,          // Uniform{w} / Trapezoid{..} / Triangle{..} / Point{..}
    pub cmq: Cmq,                  // 荷重項（§5.1 末尾）
}
/// CMQ（荷重項）：両端固定端モーメント C/M とせん断 Q。床荷重分配の検証出力。
pub struct Cmq { pub c_i: f64, pub c_j: f64, pub q_i: f64, pub q_j: f64 }

/// スラブの面荷重を、小梁を考慮して大梁荷重へ分配する。
pub fn distribute_slab(model: &squid_n_core::model::Model, slab: &squid_n_core::model::Slab)
    -> Vec<BeamLoad> { todo!() }
```

> **検証出力（R10）:** 各大梁の **CMQ図**（両端固定端モーメント・せん断）を出す。GUI（P3）で可視化。

**固定端モーメント FEM の閉形式（両端固定梁・スパン L。文献転記不要）:**

```
■ 等分布 w（一方向スラブ・全長）:          FEM = w·L²/12,        端せん断 Q = w·L/2
■ 対称三角形（ピーク w0 中央、二方向の短辺側）: FEM = 5·w0·L²/96,    Q = w0·L/4
■ 対称台形（中央が等高 w0、両端 a 区間で0→w0 に立上り、b=L−2a）:
     FEM = (w0/12)·(L² − ... )  ※台形は「三角形2つ＋中央等分布」の重ね合わせで合成する
     （実装は基本形＝等分布・三角形の FEM を区間ごとに重ね合わせて算定）
```

**CMQ の数値例（§8.4・テスト用）:** 正方形スラブ 1辺 `a=4000`, 等分布面荷重 `w=0.005 N/mm²`、
二方向（三角形分配）。ピーク強度 `w0 = w·a/2 = 10 N/mm`、`FEM = 5·w0·a²/96 = 5·10·4000²/96 =
8.3333e6 N·mm`。総和保存：`Σ部材荷重 = w·a² = 80000 N`。

### 5.2 Ai分布（ai.rs）— 設計書 §9.2/§9.3

**告示の式・値（昭和55建告1793号。法令＝公有。下記をそのまま実装）。** 調べ直す不要:

```
■ Ci = Z · Rt · Ai · C0      （C0：一次設計=0.2、二次設計=1.0）

■ Ai（高さ方向分布、告示式）:
    Ai = 1 + (1/√αi − αi) · 2T/(1+3T)
    αi = (i 階以上の地震用重量の和) / (全地震用重量)

■ Rt（振動特性係数、告示1793 第2。Tc=地盤種別の固有周期）:
    T < Tc            : Rt = 1.0
    Tc ≤ T < 2Tc      : Rt = 1 − 0.2·(T/Tc − 1)²
    2Tc ≤ T           : Rt = 1.6·Tc / T

■ Tc（地盤種別ごとの値[秒]）:  第1種 = 0.4,  第2種 = 0.6,  第3種 = 0.8

■ T（設計用一次固有周期。略算）:  T = h·(0.02 + 0.01·αs)
    h = 建物高さ[m]、αs = 鉄骨造部分の高さ比（RC造は αs=0）
    （準精算では固有値解析の実 T を代入。略算式の T をこれで置換）

■ Z（地域係数。取りうる値）:  1.0 / 0.9 / 0.8 / 0.7（沖縄県）
```

> **唯一の外部データ（公有・大量のため）:** **Z の「市町村→値」対応表**だけは告示の付表
> （数百市町村）なので、コードに埋めず**公有データファイル**として持つ（出典：告示1793 別表）。
> 上の Z 値集合・Rt/Tc/Ai/T の**式と数値は本書のとおり確定**で、調査不要。

```rust
// squid-n-load/src/ai.rs
/// 振動特性係数 Rt（告示1793 第2）。tc は地盤種別の値（0.4/0.6/0.8）。
pub fn rt(t: f64, tc: f64) -> f64 {
    if t < tc { 1.0 }
    else if t < 2.0 * tc { 1.0 - 0.2 * (t / tc - 1.0).powi(2) }
    else { 1.6 * tc / t }
}
/// 地盤種別 → Tc[秒]
pub fn tc_of(soil: SoilClass) -> f64 { match soil { SoilClass::I=>0.4, SoilClass::II=>0.6, SoilClass::III=>0.8 } }
pub enum SoilClass { I, II, III }

/// 層せん断力係数の高さ方向分布。
pub struct AiDistribution {
    pub alpha: Vec<f64>,   // αi = (i階以上の地震用重量和)/(全重量)
    pub ai: Vec<f64>,      // Ai
    pub ci: Vec<f64>,      // Ci = Z·Rt·Ai·C0
    pub qi: Vec<f64>,      // 層せん断 Qi = Ci·Wi'
    pub pi: Vec<f64>,      // 各層水平力 Pi = Qi − Q(i+1)
    pub t_used: f64,       // 使った設計用一次固有周期 T
}

/// Ai 略算式（告示）:
///   Ai = 1 + (1/√αi − αi) · 2T/(1+3T)
/// 略算では T を略算式 T = h(0.02 + 0.01·αs)（αs=鉄骨部分割合）で求める。
/// 準精算では固有値解析の実 T（squid-n-solver が渡す）を代入する。
/// 引数 `stories_weight_bottom_to_top`：階重量を **1階（最下層, index 0）→ 最上階** の順で渡す。
/// αi = (i 階以上の地震用重量和)/(全重量) なので、index 0（最下層）が最大（≈1.0）になる。
pub fn ai_distribution(stories_weight_bottom_to_top: &[f64],
                       z: f64, rt: f64, c0: f64, t: f64) -> AiDistribution { todo!() }
```

- **層せん断 → 各層水平力 `Pi = Qi − Q(i+1)`** を、層・ダイアフラムのマスター節点（重心）へ作用
  （P0 §4.5/§4.6）。
- **解析方向（R12）:** 既定 X／Y の正負、オプション 45°。**付加偏心は実装しない**（日本法令で非要求）。

### 5.3 一気通貫（squid-n-solver でオーケストレーション）— 設計書 §9.3

`squid-n-load` は純関数。**「固有値→T→Ai→水平力 LoadCase→静的」の接続は `squid-n-solver`** が行う
（squid-n-load は squid-n-solver に依存しない、を保つため）。

```rust
// squid-n-solver/src/analysis.rs
impl<'m> Analysis<'m> {
    /// 略算 or 準精算で Ai を求め、水平力 LoadCase を生成して静的解析まで通す。
    /// mode: Approx(T略算) / SemiPrecise(固有値Tを使用)
    pub fn seismic_static(&mut self, dir: SeismicDir, mode: AiMode) -> Result<StaticResult, SolveError> {
        // SemiPrecise の場合: self.eigen(1) で T1 を取得 → squid_n_load::ai_distribution(.., T1)
        //                    → 重心マスター節点へ Pi を載せた LoadCase を構築 → linear_static
        todo!()
    }
}
```

### 5.4 荷重組合せ自動生成（combo.rs）— 設計書 §9.1

- 法令の標準組合せ（長期・短期：固定＋積載＋地震／積雪…）を**自動生成**（係数は外部化）＋手動追加可。
- **組合せ名の表記**：`standard_combinations`・`default_combinations` とも、組合せ名は荷重ケースの
  直接的な名前で表す。算定式との対応は G→DL・P→LL・K→EX/EY・W→WX/WY・S→SL
  （積雪は単一ケースのため方向なし。例 `DL+LL`・`DL+LL±EX`・`DL+LL±WX`・`DL+LL+0.35SL±EX`）。
  長短期判別 `is_short_term_combo` は地震（E／旧名 K）・風（W）・積雪（S 係数）で判定し、
  旧名（`G+P±Kx` 等）の保存データも後方互換で判定できる。
- **新規モデルの既定組合せ**：`squid_n_core::model::default_combinations`（`with_default_load_cases`
  が設定）が、標準荷重ケース（DL・LL(架構用)・LL(地震用)・EX・EY）に対応する
  長期 `DL+LL` と短期地震 `DL+LL±EX`・`DL+LL±EY` の 5 組合せを初期状態で用意する。
  これは `squid_n_load::combo::auto_combinations`（DL/LL/EX/EY 指定）と名前・係数構成とも完全一致で、
  `combo.rs` の `test_default_combinations_matches_auto_combinations` が保証する。暴風・積雪・
  多雪区域の係数付き組合せは UI の「標準組合せを生成」で追加する。

**DoD（T3/T4）:**
- 矩形スラブで三角形／台形分配の**総和が床総荷重に一致**（質量・荷重保存。厳密）。
- 小梁経由の反力が手計算と一致。CMQ が荷重項の理論値と一致（§7.3、厳密）。
- Ai 式の自己整合（§7.4 の計算例）と**告示計算例**の Ci・Qi 一致。
- 準精算：固有値由来 T を入れると Ai 分布が略算と変わる（§7.5）。

---

## 6. T5: 結果 Parquet I/O（squid-n-io）

設計書 §12.3。結果を**列指向（Arrow/Parquet）**で格納。**P2 規模（静的＋モーダル＝小規模）で実装・検証**。

```
results/
├── index.json               # マニフェスト（ケース・種別・行数・範囲）
└── case_<id>/
    ├── nodal_disp.parquet    # 節点変位・反力
    ├── member_force.parquet  # 部材内力（評価位置別）
    ├── story.parquet         # 層せん断・層間変形（剛性率/偏心率は P7）
    └── modal.parquet         # 固有周期・刺激係数・有効質量・モード形
```

```rust
// squid-n-io/src/results.rs（設計書 §12.3 の ResultStore）
pub type CaseId = u32;   // 解析ケース識別（荷重ケース/組合せ/モーダル等に採番）

/// 結果の種別（= Parquet ファイル種別）。
#[derive(Clone, Copy, Debug, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub enum ResultKind { NodalDisp, MemberForce, Story, Modal }

/// 部分読み出しのフィルタ。指定が None の軸は全件。
pub struct ResultQuery {
    pub case: CaseId,
    pub kind: ResultKind,
    pub node_filter: Option<Vec<squid_n_core::ids::NodeId>>,
    pub member_filter: Option<Vec<squid_n_core::ids::ElemId>>,
    pub step_range: Option<(u64, u64)>,   // 時刻歴(P6)向け。静的/モーダルでは None
}

/// 読み出し結果（Arrow の RecordBatch ラッパ）。列名→列で受け渡し。
pub struct ResultBatch { pub batch: arrow::record_batch::RecordBatch }

/// 1ケース・1種別へ行を追記するストリーミング書込ハンドル。
pub trait ResultWriter {
    fn write_rows(&mut self, batch: &arrow::record_batch::RecordBatch); // RowGroup 単位で追記
    fn finish(self: Box<Self>);                                          // フッタ確定・クローズ
}

/// 全体マニフェスト（index.json）。ケース・種別・行数・範囲。
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct ResultManifest {
    pub entries: Vec<ResultEntry>,
}
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct ResultEntry { pub case: CaseId, pub kind: ResultKind, pub rows: u64, pub path: String }

pub trait ResultStore {
    fn writer(&mut self, case: CaseId, kind: ResultKind) -> Box<dyn ResultWriter>;
    fn query(&self, q: &ResultQuery) -> ResultBatch;  // 部分読み出し
    fn manifest(&self) -> &ResultManifest;
}
```

**arrow/parquet 59 の実 API（確認済み・そのまま使える）。** 調べ直す不要:

```rust
// squid-n-io/src/results_parquet.rs
use parquet::arrow::arrow_writer::ArrowWriter;
use parquet::file::properties::WriterProperties;
use arrow::record_batch::RecordBatch;
use arrow::datatypes::SchemaRef;
use std::{fs::File, sync::Arc};

// --- ストリーミング書込（ResultWriter 実装）---
pub struct ParquetWriter { inner: ArrowWriter<File> }
impl ParquetWriter {
    pub fn create(path: &str, schema: SchemaRef) -> parquet::errors::Result<Self> {
        let file = File::create(path).unwrap();
        let props = WriterProperties::builder()
            .set_max_row_group_size(64 * 1024)   // RowGroup の行数上限（部分読出しの粒度）
            .build();
        // 確定 API: ArrowWriter::try_new(writer, schema, Some(props))
        Ok(Self { inner: ArrowWriter::try_new(file, schema, Some(props))? })
    }
}
impl super::results::ResultWriter for ParquetWriter {
    fn write_rows(&mut self, batch: &RecordBatch) { self.inner.write(batch).unwrap(); } // バッチ追記
    fn finish(self: Box<Self>) { self.inner.close().unwrap(); }                          // フッタ確定
}
// 必要に応じ inner.flush() で早期に RowGroup を切り出す。

// --- 部分読出し（特定 RowGroup・特定列のみ）---
use parquet::arrow::arrow_reader::ParquetRecordBatchReaderBuilder;
use parquet::arrow::ProjectionMask;
pub fn read_partial(path: &str, row_groups: Vec<usize>, columns: Vec<usize>)
    -> parquet::errors::Result<Vec<RecordBatch>>
{
    let file = File::open(path).unwrap();
    let builder = ParquetRecordBatchReaderBuilder::try_new(file)?;
    let mask = ProjectionMask::roots(builder.parquet_schema(), columns); // 列射影
    let reader = builder
        .with_row_groups(row_groups)   // 必要な RowGroup だけ読む（ステップ範囲→RowGroup に対応づけ）
        .with_projection(mask)         // 必要な列だけ読む（部材/節点フィルタ）
        .build()?;
    reader.collect::<Result<Vec<_>, _>>().map_err(Into::into)
}
```

- **ストリーミング書込:** `ArrowWriter::write(&batch)` を繰り返し、`close()` で確定。RowGroup 単位で
  追記され全保持しない（時刻歴 P6 で本領発揮、P2 でも同方式）。
- **部分読出し:** `with_row_groups`＋`with_projection` で「ケース X・部材 Y・ステップ範囲 Z」だけ取得
  （`ResultQuery` を RowGroup 番号・列マスクに翻訳する。ステップ範囲↔RowGroup の対応は index.json に記録）。

> **DoD の規模に注意（§0.4）:** §12.6 の「1GB級→100ms」は時刻歴（P6）の話。**P2 のテストは
> P2 が生む結果（小規模）で**、ストリーミング書込→部分読出しが**機能すること**を確認する。

**DoD（T5）:** 静的・モーダル結果を Parquet に書き、特定ケース・特定部材のみ読み出せる。
Python/pandas で開ける（相互運用の確認）。

---

## 7. T6: テスト・DoD（フェーズ全体）

### 7.1 線形静的（→ T1）
- 単純梁・1層ラーメンで変位・応力が手計算一致（厳密 1e-9）。3ケースが1分解で解け、組合せ＝線形結合。

### 7.2 固有値（→ T2）★許容差／ビット一致を区別
- 1自由度 `T=0.198692 s`、2層せん断モデル `T1=0.32150, T2=0.12280 s` に**収束**（相対 1e-8）。
- 有効質量比合計 ≈100%（2層は2モードで 100%）。
- 実行間ビット一致（固定シード・単一スレッド）。

### 7.3 床荷重・CMQ（→ T3）★厳密一致
- 正方形スラブ（1辺 `a=4000`, `w=0.005 N/mm²`）三角形分配:
  - 総和保存：`Σ部材荷重 = w·a² = 0.005·16e6 = 80000 N`（厳密）。
  - 各大梁ピーク強度 `w0 = w·a/2 = 10 N/mm`、`FEM = 5·w0·L²/96 = 5·10·4000²/96 = 8.3333e6 N·mm`。
- 台形・一方向・負担面積でも総和保存。

### 7.4 Ai（→ T4）
- **式の自己整合（計算例・3層等重量, T=0.24 s）:** 階重量を 1階→最上階で `[W, W, W]` と渡すと
  `α=[1.0, 0.6667, 0.3333]`（index 0＝1階が最大）、`2T/(1+3T)=0.27907` →
  `A=[1.000, 1.156, 1.390]`（1階→最上階。この数値は式への代入＝確認用）。
- **告示計算例照合（任意の追加確認）:** Z・Rt・C0 は §5.2 の確定式・値で算定可能。告示の公式計算例が
  あれば Ci・Qi をクロスチェック（無くても式の自己整合で判定できる）。

### 7.5 一気通貫（→ T4/§5.3）
- 略算 T と固有値由来 T で Ai 分布が変わることを確認。準精算の水平力で静的解析が完走し、
  層せん断が `Qi` と整合。

### 7.6 結果I/O（→ T5）
- 静的・モーダル結果を Parquet 書込→部分読出し（ケース・部材指定）。

### 7.7 フェーズ DoD チェックリスト
| # | ゴール（§0.2） | 判定 | 章 |
|---|---|---|---|
| 1 | 線形静的 手計算一致・多ケース1分解 | §7.1 | §3 |
| 2 | 固有値 理論収束＋有効質量≈100％ | §7.2 | §4 |
| 3 | 告示 Ai 一致 | §7.4 | §5.2 |
| 4 | CMQ・荷重保存（厳密） | §7.3 | §5.1 |
| 5 | 一気通貫（固有値→Ai→静的） | §7.5 | §5.3 |
| 6 | 結果 Parquet 部分読出し | §7.6 | §6 |

> **全チェック緑＝ P2 完了（v0.5 の中核「単一モデルで静的＋振動」を達成）。** 次は P3（最小UI/設計）。

---

## 8. 数式・理論補足（ジュニア向け）

### 8.1 質量行列（→ §2）
- **集中質量**：要素質量を端部並進へ配分した対角行列。簡便・高速だが回転質量 0 で M が特異。
- **整合質量**：剛性と同じ形状関数から作る密行列。SPD。精度が高いが計算量増。

### 8.2 一般化固有値と部分空間反復（→ §4）
振動は `K φ = ω² M φ`（`λ=ω²`）。固有周期 `T=2π/ω`。下位モードだけ要るので、q 次元の部分空間で
`K Y = M X` を反復し、小さな q×q 問題に縮約（Rayleigh–Ritz）して下位 n 個へ収束させる。**K は SPD のまま
分解**できるのが利点（P0 Cholesky を再利用）。反復解法なので「理論への一致」は許容差で見る。

### 8.3 Ai 分布（→ §5.2）
地震層せん断力係数の高さ分布。`Ci = Z·Rt·Ai·C0`。`αi` は i 階以上の重量比で、上階ほど割増（Ai>1）。
`Ai = 1 + (1/√αi − αi)·2T/(1+3T)`。T が大きいほど上階割増が強い。略算は T を略算式で、準精算は
固有値解析の実 T を使う（同じ式に、より正確な T を入れる）。

### 8.4 CMQ（荷重項）と固定端モーメント（→ §5.1）
部材荷重による両端の固定端モーメント C/M とせん断 Q。床の二方向分配では、各大梁は三角形／台形の
分布荷重を受ける。対称三角形荷重（ピーク `w0` が中央）の固定端モーメントは `FEM = 5 w0 L² / 96`
（§5.1 に閉形式を明示済み：等分布 wL²/12・対称三角形 5w0L²/96）。総和は必ず床総荷重に一致する
（保存則＝最良の検算）。

### 8.5 三角形・台形分配（45°法）（→ §5.1）
二方向スラブは、各辺から 45°線を引いて床を4分割し、短辺側は三角形、長辺側は台形の荷重として
隣接大梁へ流す近似。正方形なら4辺すべて三角形。面積（＝荷重）の総和は床全体に一致する。

---

## 9. 用語（P2 で増えるもの）
| 用語 | 意味 |
|---|---|
| Analysis API | 1回の分解で複数荷重ケース・組合せを解く線形静的の本体 |
| 固有値解析 | `Kφ=λMφ` を解き固有周期・モード形を得る |
| 部分空間反復法 | 下位モードを K 単独分解の反復で求める方法（SPD 維持） |
| 集中／整合質量 | 質量行列の2方式。集中=対角・特異、整合=密・SPD |
| 刺激係数 | 各モードが地震入力にどれだけ励起されるかの係数 |
| 有効質量比 | 各モードが担う質量の割合。総和≈100% |
| Ai 分布 | 地震層せん断力係数の高さ分布。`Ci=Z·Rt·Ai·C0` |
| 略算／準精算 | T を略算式で／固有値の実 T で求める Ai |
| 一気通貫 | 固有値→Ai→静的を単一モデルで自動接続 |
| CMQ（荷重項） | 部材荷重による両端固定端モーメント・せん断 |
| 三角形・台形分配 | 二方向スラブ荷重の 45°法による大梁への分配 |
| Parquet | 列指向の結果格納形式。部分読出し・pandas 連携 |

---

*本仕様書は P2（線形解析＋荷重）を対象とする。shift-invert Lanczos／SRSS・CQC 精算／時刻歴規模の*
*結果I/O は将来フェーズ。次フェーズは P3（最小UI／許容応力度設計）。*
