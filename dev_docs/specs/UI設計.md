# UI 設計 実装仕様書（横断）

**位置づけ:** フェーズ横断の **UI 全体設計**。実装計画で抜けていた UI 構成を補完する。
**対象読者:** UI 担当リード＋フロントエンド実装者（Rust ジュニア）
**親文書:** `構造計算一貫プログラム_実装設計書.md`（以降「設計書」。`§x.y` はその章番号）
**関連フェーズ:** [P3 最小UI/設計](P3_最小UIと設計.md)（本書で工程タブ化に改訂）・[P8 操作・連携](P8_操作と連携.md)
（GUI拡充・MCP編集の共通経路）・[P0](P0_基盤.md)（断面データ）・[P4](P4_材料と断面.md)（配筋）
**前提環境:** `egui = "0.34"` / `eframe = "0.34"` / `egui_extras = "0.34"` / 3D は `wgpu = "29"`

---

## 0. このドキュメントについて

### 0.1 目的

GUI の**全体構成・工程フロー・主要インタラクション**を確定し、P3/P8 の GUI 実装が一貫した土台に
乗るようにする。本書の決定は、関連フェーズへ反映する（§9）。

### 0.2 確定事項サマリ（本書で決定）

1. **4ペイン構成＋工程タブ**（モデル→荷重→解析→結果→設計→レポート、進行ロックなし）。§1
2. 各工程タブの中身。§2
3. **3D選択→断面編集（共有編集＋複製ボタン）**。§3
4. **断面作成UI（パラメトリック `SectionShape`）**：鋼 H/箱/L/C/T/丸、RC 矩形/丸＋XY主筋＋せん断補強筋。§4
5. **stale（要再計算）表示＋手動再計算トリガ**。§5
6. 結果可視化の種類（検定比＝断面算定結果を含む）。§6
7. 入力支援（コピペ・一括・即時バリデーション）。§7
8. レポート構成。§8

---

## 1. 全体構成（ドック型4ペイン＋工程タブ）

```
┌──────────────────────────────────────────────────────┐
│ ファイル | [モデル][荷重][解析][結果][設計][レポート] | Undo/Redo | 長期/短期 │
├──────────────┬────────────────────────┬──────────────┤
│ 左ドック      │  中央：3Dビュー（モデル/荷重/解析） │ 右ドック        │
│ 切替:        │  結果/設計/レポートは各ビュー      │ 切替:          │
│ ・ナビゲータ   │                        │ ・インスペクタ    │
│ ・作成パレット  ├────────────────────────┤ ・解析設定       │
│              │ 下ドック（タブ）：ログ/診断/モデル/荷重│               │
├──────────────┴────────────────────────┴──────────────┤
│ 🗂✏📜📋⚡⚠ | ファイル名 | ⏳ジョブ | stale | ⚠エラー(1行)  サマリ ⚙🔍 │
└──────────────────────────────────────────────────────┘
```

左・右・下の 3 領域は Zed 風の**開閉可能ドック**で、ステータスバーのアイコンが
「アイコン＝パネル」として機能する（対象ドックが開いていてそのパネルがアクティブなら閉じる。
それ以外は開いてそのパネルをアクティブにする）:

- 左端: 🗂 ナビゲータ・✏ 作成パレット（左ドック）／📜 ログ・📋 モデル表・⚡ 荷重表・⚠ 診断（下ドック）
- 右端: ⚙ 解析設定・🔍 インスペクタ（右ドック）

レイアウトは egui 標準パネル（`egui::Panel`）の `show_inside` を
top（ツールバー）→ bottom（ステータスバー）→ left → right → bottom（下ドック）→ central の順で
呼んで構成する（下ドックを左右ドックの**間**＝中央領域の下部に出すため、左右より後に呼ぶ）。
左右ドックは egui のパネルリサイズでドラッグ幅調整できる（左 180–520px、右 220–560px）。

各領域の内容:

- **左ドック**: ナビゲータ（§1.2）／作成パレット（梁・壁・スラブ作成モードと断面割当。
  3D クリックと連動する編集ツール群）。
- **右ドック**: インスペクタ（選択要素の諸元・断面編集・検定サマリ）／解析設定
  （CollapsingHeader のセクション群。3D を見ながら設定・実行できるよう右ドックに置く。
  全体を縦スクロール化し、横並びは折り返しで狭幅に対応）。
- **下ドック**: ログ（§1.3）／診断（§1.4）／モデル編集テーブル（節点・部材・断面等の
  サブタブ群）／荷重編集テーブル。横長テーブルは幅の狭い左ドックより下ドックの方が
  視認性が良いためここに置く。
- **中央**: モデル/荷重/解析タブでは 3D ビュー。結果/設計/レポートタブでは各ビュー。

**工程タブはドック配置のプリセット**として機能する: タブクリックで各工程に適した
初期配置（例: モデル→下ドックにモデル表、解析→右ドックに解析設定）へ切り替えるが、
以後のドック開閉・パネル切替の手動変更は妨げない。

### 1.1 横断ルール（構造ソフト特有）

- **工程タブはフロー順だが進行ロックしない**。構造設計は反復（断面変更→再計算）なので、いつでも前工程へ
  戻れ、モデルは常に編集可能。
- **3D とモデル状態は常時アクセス可能**。ナビゲータ選択 ⇄ 3Dピック ⇄ テーブル行が**双方向連動**。
- **stale（要再計算）の可視化**（§5）。
- 全編集は**編集トランザクション**（P3 §3.3）経由＝Undo可・MCP（P8）と共通経路（単一ライタ R29）。

```rust
// squid-n-app/src/app.rs（P3 のタブを工程ベースに改訂）
pub enum Tab { Model, Loads, Analysis, Results, Design, Report } // 旧 Nodes/Members/... を置換
pub struct App {
    pub model: squid_n_core::model::Model,
    pub results: Option<ResultsBundle>,   // P3 §2 で定義
    pub selection: Selection,             // P3 §2 で定義（ナビ/3D/表 連動）
    pub undo: squid_n_edit::UndoStack,         // P8 §2 で squid-n-edit へ移設済み
    pub active_tab: Tab,
    pub staleness: Staleness,             // §5
    pub nav: Navigator,                   // §1.2
}
```

### 1.2 ナビゲータ（左ドック）

```rust
pub struct Navigator { /* 階/レベル・部材グループ・荷重ケース・結果ケースのツリー */ }
```
- ツリーのノード選択で中央（3D/テーブル）の対象を絞り込み・ハイライト。
- 3Dピック・テーブル選択と `Selection` を共有して同期。

### 1.3 イベントログ（下ドック）

セッション内の出来事（エラー・注意・ジョブの開始/完了）を時系列で保持し、下ドックに表示する。

```rust
// squid-n-app/src/app/mod.rs
pub enum LogLevel { Info, Notice, Error }
pub struct LogEntry { pub elapsed: std::time::Duration, pub level: LogLevel, pub message: String }
pub struct EventLog { /* started: Instant, entries: Vec<LogEntry>（上限 1000 件） */ }
```

- 時刻は**アプリ起動からの経過時間**（「mm:ss」表示）。std のみでは壁時計のローカル時刻表記が
  できないため（タイムゾーン情報が取れない）、依存を増やさずセッションログとして十分な経過時間を採る。
- メッセージの発行は `App::report_error / report_notice / report_info` に一元化する。
  - `report_error`: `last_error`（ステータスバー1行表示・赤）＋ログ。**下ドックを自動で開く**
    （エラーが埋もれると解析結果を誤って信頼しかねないため）。
  - `report_notice`: `last_notice`（黄・処理継続可の注意）＋ログ。自動オープンしない。
  - `report_info`: ログのみ（ジョブの開始「⏳」・完了「✅ (経過秒)」など）。
- ステータスバーの ⚠ エラー1行表示はクリックで下ドックを開く（全文・履歴の確認導線）。
- ログの各行は1行に畳んで truncate 表示し、全文はホバーで表示（ステータスバーと同じ流儀）。

### 1.4 診断（下ドック）

モデル整合性チェックの結果を重要度付きで一覧表示する（エディタの problems パネルに相当）。

```rust
pub enum DiagSeverity { Error, Warning, Info }
pub enum DiagTarget { Node(NodeId), Member(ElemId) }
pub struct Diagnostic { pub severity: DiagSeverity, pub message: String, pub target: Option<DiagTarget> }
```

- チェック内容（`App::run_diagnostics`。O(部材数) 程度の軽い検査に限る）:
  モデル検証（`Model::validate`）／支点なし／断面未割当（100 件超は集約）／
  空の地震荷重ケースを参照する荷重組合せ／荷重が空の荷重ケース。
- **遅延評価**: 編集で `Staleness::diagnostics_stale` を立て、診断タブを開いた時点で
  stale なら再実行する（編集のたびに毎フレーム走らせない）。「再チェック」で手動実行も可能。
- `target` を持つ行はクリックで 3D 選択（`Selection`）とインスペクタ（`Navigator::focus_*`）へ反映。
- タブラベルに件数を表示（例「診断 (E1/W3)」）。

---

## 2. 各工程タブの中身

| タブ | 中央 | テーブル/操作 |
|---|---|---|
| **モデル** | 3D形状 | 節点／部材（柱・大梁・小梁）／断面／材料／階(レベル)／スラブ／拘束（支点・剛床・MPC）。**断面作成UI（§4）はここ**。通り芯グリッドから一括生成（§7） |
| **荷重** | 3D(荷重表示)＋**CMQ図** | 床面荷重（スラブ別・固定/積載[床/架構/地震]/積雪）・小梁配置／荷重ケース／組合せ（自動＋手動）／地震設定（Z・地盤種別・C0・方向・Ai略算/準精算）。P2 §5 |
| **解析** | 3D（右ドックに設定） | 種別チェック（線形静的／固有値[モード数]／プッシュオーバー[分布・制御・方向]／時刻歴[波・減衰]）＋幾何剛性ON/OFF・決定性。**実行→ジョブ進捗**（P8 §5 非同期と共通）。設定フォームは右ドックの解析設定パネル |
| **結果** | 3D＋グラフ | §6 の可視化（**検定比色分け＝断面算定結果を含む**）＋ナビゲータの結果ケース選択 |
| **設計** | 検定比表＋3D連動 | 一次(許容応力度 P3)／二次(保有耐力・Ds・剛性率/偏心率 P7)／限界耐力(P12)。位置別・組合せ別・色分け・内訳(σ/許容/条文)。NG部材→3Dハイライト→**断面編集へジャンプ**（反復動線、§3） |
| **レポート** | プレビュー | 章選択→計算書出力（§8） |

---

## 3. 3D選択 → 断面編集（共有編集＋複製）

- 3D/ナビゲータ/テーブルで部材選択 → **右インスペクタに断面（`SectionShape`＋算定プロパティ）を表示**。
- **編集はその断面（SectionId）を参照する全部材に波及**。インスペクタに
  **「この断面を使う N 部材に影響します」**と影響数を常時表示（誤爆防止）。
- 並びに**「複製してこの部材だけ別断面に」ボタン** → 新 SectionId を生成し当該部材のみ再割当。
- いずれも `EditCommand`（P3 §3.3）として適用 → **Undo可・MCP と共通**。

```rust
// squid-n-app/src/command.rs（編集コマンドの例。squid-n-edit に置く）
pub struct EditSectionShape { pub section: SectionId, pub new_shape: SectionShape } // 共有断面を編集（波及）
pub struct DuplicateSectionForMember { pub member: ElemId }                          // 複製→当該部材のみ再割当
```

**DoD:** 断面編集が共有部材に波及し影響数が出る。複製ボタンで当該部材のみ別断面化。両操作が Undo 可能。

---

## 4. 断面作成UI（パラメトリック `SectionShape`）

### 4.1 断面タイプとパラメータ

```
鋼材:
  H形    : H(せい), B(幅), tw(ウェブ厚), tf(フランジ厚), r(フィレット任意)
  箱形/角形鋼管 : H, B, t（板厚）
  L形(山形)     : A, B(脚長), t
  C形(溝形)     : H, B, tw, tf
  T形           : H, B, tw, tf
  円形(鋼管)    : D(径), t（板厚）

RC:
  矩形 : B × D
  円形 : D
  配筋:
    主筋   : X方向・Y方向 別に〔本数・径(D10〜D41)・段数・かぶり〕
    せん断補強筋(帯筋/あばら筋) : 径・ピッチ・組数(脚数)
```

- **断面プレビュー**（寸法図＋RCは配筋図）をインスペクタ内に描画。
- 規格断面ライブラリ（JIS 形鋼）からの選択は**任意拡張**（パラメトリック入力が基本）。

### 4.2 データモデル（新設 `SectionShape`）

現行 `squid-n-core::Section` は**計算済みプロパティ**（A/I/J/As/depth/width…）。その上流に
**パラメトリック定義 `SectionShape`** を新設し、プロパティを**自動算定**して `Section` を生成する。

```rust
// squid-n-core/src/section_shape.rs（新設）
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub enum SectionShape {
    SteelH    { h: f64, b: f64, tw: f64, tf: f64, r: f64 },
    SteelBox  { h: f64, b: f64, t: f64 },          // 角形鋼管
    SteelAngle{ a: f64, b: f64, t: f64 },          // L形
    SteelChannel { h: f64, b: f64, tw: f64, tf: f64 }, // C形
    SteelTee  { h: f64, b: f64, tw: f64, tf: f64 },
    SteelPipe { d: f64, t: f64 },                  // 円形鋼管
    RcRect    { b: f64, d: f64, rebar: RcRebar },
    RcCircle  { d: f64, rebar: RcRebar },
}

#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct RcRebar {
    pub main_x: BarSet,   // X方向 主筋
    pub main_y: BarSet,   // Y方向 主筋
    pub cover: f64,       // かぶり
    pub shear: ShearBar,  // せん断補強筋
}
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct BarSet { pub count: u32, pub dia: f64, pub layers: u32 } // 本数・径・段数
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct ShearBar { pub dia: f64, pub pitch: f64, pub legs: u32 } // 径・ピッチ・組数

impl SectionShape {
    /// 形状・寸法から断面性能（A, Iy, Iz, J, As_y, As_z, depth, width）を算定し Section を作る。
    /// 鋼の閉形式・矩形/円の積分は明示（実装で埋める）。
    pub fn to_section(&self, id: SectionId, name: String) -> Section { todo!() }
}
```

- **`Section` は `SectionShape` の派生**（`SectionShape` を保持し、プロパティはキャッシュ）。寸法変更で再算定。
- **配筋 `RcRebar`** は P4 `Reinforcement`（[P4 §7](P4_材料と断面.md)）へ供給：ファイバ断面・スケルトン・
  P7 の Ds 耐力比に使う。弾性剛性は原則 gross（ひび割れ考慮は P4/非線形側）。

**DoD（§4）:** 各タイプで寸法→A/I/J/As が手計算一致（矩形 `A=BD`, `Iz=BD³/12` 等。鋼Hは部材表と照合）。
RC 配筋がプレビューに反映され、P4 のファイバ断面へ渡る。

---

## 5. stale（要再計算）と実行トリガ

**方針：手動再計算＋stale表示**（重い解析を勝手に走らせない）。

- モデル/荷重編集 → 依存する下流（結果・設計）に **「要再計算」バッジ（オレンジ）**。
- 工程タブに状態アイコン **✓最新 ／ ⚠stale ／ ▷未実行**。
- 主トリガは**解析タブの実行ボタン**。結果/設計タブ上部に「最新解析時刻 ／ 現モデルと不一致」を表示。
- **依存追跡**は編集トランザクションのスコープから判定（例：ある断面の変更→その断面を使う部材の結果のみ stale）。
- **部分再計算**：荷重のみ変更は後退代入のみ（同一 K 再利用、P2 §3）、断面/形状変更は再組立。
  決定性（R28）・チェックポイント（P6 §5）と整合。

```rust
// squid-n-app/src/app.rs
pub struct Staleness { pub results_stale: bool, pub design_stale: bool, pub last_run: Option<std::time::SystemTime> }
```

**DoD（§5）:** 編集で下流が stale 表示。実行で最新化。荷重のみ変更は部分再計算で速い。

---

## 6. 結果可視化の種類

- 変形図（静的・モード、倍率スライダ／アニメ）
- 部材応力図 **N／Q／M**（部材ローカル・評価位置、P1 §6.2.3）
- **CMQ図**（荷重項、フェースモーメント位置併記、P2 §5.1）
- シェル応力コンター（膜力・モーメント、P1.5）
- モード形（モード選択・固有周期表示、P2 §4）
- **ヒンジ状態**（crack/yield/ultimate 色分け・発生順アニメ、P5 §7.4）
- **性能曲線**（容量曲線・性能点、P12 §5）
- 時刻歴グラフ（節点応答・層せん断・層間変形角の時系列、P6）
- 層レベル（層せん断–層間変形・剛性率・偏心率）
- **検定比色分け（＝断面算定結果）**：緑/黄/赤、NG部材ハイライト、クリックで内訳（設計タブと連動）
- 共通：凡例・カラースケール・断面位置プローブ。

### 6.1 表示対象（荷重ケース／組合せ）の選択

解析済みの結果（`ResultsBundle.statics`＝ケース別・`combos`＝組合せ別）から、表示・断面算定の
対象を選ぶ。選択手段は3つで、いずれも `App::select_displayed_result(StaticKey)` を呼ぶ:

- 左ナビゲータ「結果ケース」（`panels.rs`）
- 結果タブ上部の「表示対象:」ドロップダウン
- 設計タブ上部の「対象荷重:」ドロップダウン

`select_displayed_result` は、変位図・層指標が参照する `focus_result` の切替に加え、応力図
（N/Q/M）・断面検定が参照する `ResultsBundle.member_forces` を選択結果の内力へ差し替える。
荷重組合せを選んだ場合は、その名前から `is_short_term_combo` で長期/短期区分（`design_term`）を
再判定し、断面検定を再実行する。これにより、**選んだ荷重組合せの内力と長期/短期に応じた
断面算定結果**が結果タブ・設計タブに表示される（全組合せ一括解析後も、再解析せず対象を切替できる）。

**DoD（§6）:** 各可視化が対応フェーズの結果から描ける。検定比色分けが設計結果と一致し 3D/表が連動。
表示対象の切替で応力図・断面検定（長期/短期）が追従する。

---

## 7. 入力支援

- スプレッドシート同等：**コピー&ペースト（Excel往復）・一括編集・フィル・Undo/Redo**。
- **即時バリデーション**：不正セル赤背景＋理由ツールチップ（`squid_n_core::Model::validate` と同規則、P0 §3.11）。
- 一括生成：通り芯グリッド→節点・部材自動生成、階の複製、断面の一括割当。
- 単位：入力＝日本慣用、内部 N·mm·s、境界変換（P0 §3.1）、表示単位の切替。
- 仮想スクロール（数万行、egui_extras `body.rows`、P3 §3.1）・フィルタ/ソート/検索。
- CSV 入出力（P8 §7）。

**DoD（§7）:** 1万部材でコピペ・一括・スクロールが滑らか。不正入力が即赤表示。

---

## 8. レポート構成

1. 表紙・建物概要・**準拠規準版＋責任範囲明記**（P9 §5.1）
2. 入力一覧（節点・部材・断面・材料・荷重・組合せ）
3. 荷重（床荷重・CMQ・地震力 Ai/Ci/Qi）
4. 解析結果（変位・反力・部材応力・固有周期・層間変形）
5. 設計（検定比一覧[色分け]・Ds・剛性率/偏心率・保有耐力・限界耐力）
6. 図（軸組図・伏図・応力図・検定比図・CMQ図）
- 形式：**PDF（計算書）/ Excel（表）/ CSV**、章選択・テンプレート、適用条文明記。

**DoD（§8）:** 章を選んで PDF/Excel を出力。図表が解析・設計結果と一致。出典・条文が明記される。

---

## 9. 実装スケジュール（いつ・どの順で実装するか）

> UI は単独フェーズではなく、**各フェーズに UI タスクを差し込む**形で段階導入する。下表で
> 「どの UI 項目を・どのフェーズで・何を前提に・どのタスク（UI-x）として・何を満たせば完了か」を確定する。
> 各 UI-x タスクは、対応フェーズの仕様書のタスク一覧に**追加して**実装する（§9.5 の反映先）。

### 9.1 着手の大原則（順序の根拠）

1. **データが先、UI が後**：UI が編集・表示する対象（`SectionShape` 等）を先にデータ層へ入れる。
2. **土台 → 機能拡充**：4ペイン・工程タブ・3D・編集トランザクション・stale は P3 で**土台**を作り、
   以降のフェーズで結果が出るたびに**結果可視化を足す**。
3. **断面タイプは増分**：鋼 H形・RC 矩形＋基本配筋を先行。他タイプは `SectionShape` の enum 追加で順次。

### 9.2 v0.5 期（UI 土台）— ★ここが UI 実装の主戦場

| UI-ID | UI項目 | 実装する場所/時期 | 前提 | 完了条件(DoD) |
|---|---|---|---|---|
| **UI-D1** | `SectionShape`/`RcRebar`/`BarSet`/`ShearBar` データ型新設、`Section` を派生化、`to_section`（**鋼H形・RC矩形のみ**算定） | **squid-n-core／squid-n-section に追加**（P3 着手の**直前**＝P3 の T0 として） | P0 完了済 | H形・矩形で A/Iy/Iz/J/As が手計算一致（矩形 `Iz=BD³/12` 等）。serde ラウンドトリップ |
| **UI-1** | 4ペイン枠・工程タブ（Model/Loads/Analysis/Results/Design/Report）・自由遷移・下部ステータス | **P3 T0〜T1 を本構成へ改訂** | UI-D1 | タブ切替・3ペイン表示・進行ロックなし |
| **UI-2** | ナビゲータ（階/部材群/ケース）＋ 3D/テーブル/ナビの双方向連動・選択同期 | **P3 T4（3D）＋T1（表）に統合** | UI-1 | ナビ選択⇄3Dピック⇄表行が同期 |
| **UI-3** | 断面作成UI（鋼H形・RC矩形＋主筋/せん断補強筋）＋寸法・配筋プレビュー | **P3 T1 内（モデルタブ）** | UI-D1 | 入力→`to_section`→性能反映、プレビュー描画 |
| **UI-4** | 3D選択→右インスペクタで断面編集（**共有編集＋影響数表示＋複製ボタン**）、`EditCommand` 経由 | **P3 T2（編集トランザクション）に統合** | UI-2,UI-3 | 波及・影響数・複製・Undo が動く（§3 DoD） |
| **UI-5** | 即時バリデーション（赤＋理由）・コピペ・一括生成（通り芯グリッド）・仮想スクロール | **P3 T1/T2** | UI-1 | 1万部材で快適、不正即赤（§7 DoD） |
| **UI-6** | stale 表示＋手動再計算トリガ＋部分再計算（荷重のみ後退代入） | **P3 T3（解析実行）＋各結果タブ** | UI-1, P2 | 編集で下流 stale、実行で最新化（§5 DoD） |
| **UI-7** | 結果可視化(基本)：変形図・N/Q/M・CMQ図・固有モード・**許容応力度の検定比色分け＋表** | **P3 T5（応力/CMQ）＋ T7（検定比表）** | P2, P3設計 | 単純梁で図が理論形、検定比が設計と一致 |

> v0.5 完了時点で「**工程タブ・4ペイン・断面作成(H/矩形)・3D断面編集・stale・一次設計の結果/検定可視化**」が
> 動く。これが UI の完成形の骨格。

### 9.3 v1.0 期（機能が出るたび結果可視化・断面タイプを足す）

| UI-ID | UI項目 | 実装フェーズ | 前提 |
|---|---|---|---|
| **UI-8** | `RcRebar` → P4 `Reinforcement` 連携（断面作成UIの配筋がファイバ/スケルトンへ） | **P4 T5（スケルトン）と同時** | UI-D1, P4 |
| **UI-9** | 断面タイプ追加：箱形・L形・C形・T形・円形（`SectionShape` enum 追加＋`to_section`） | **P4 期（材料・断面の充実と同時）** | UI-D1 |
| **UI-10** | ヒンジ状態（crack/yield/ultimate 色分け・発生順アニメ）・性能曲線 | **P5（プッシュオーバー）と同時** | UI-7, P5 |
| **UI-11** | シェル応力コンター（膜力・モーメント） | **P1.5（板要素）と同時** | UI-7, P1.5 |
| **UI-12** | 時刻歴グラフ（節点応答・層せん断・層間変形角の時系列） | **P6（時刻歴）と同時** | UI-7, P6 |
| **UI-13** | 設計タブ拡充：保有耐力・Ds・剛性率/偏心率。**幅厚比を `SectionShape` 寸法から算定** | **P7（二次設計）と同時** | UI-7, P7 |
| **UI-14** | 断面編集・複製の `EditCommand` を MCP `model.edit` からも生成（GUI と共通経路） | **P8（MCP）と同時** | UI-4, P8 |
| **UI-15** | CSV 入出力・解析の非同期ジョブを GUI から（進捗表示） | **P8 と同時** | UI-6, P8 |
| **UI-16** | レポート出力（PDF/Excel/CSV・章選択・図表）（§8） | **P9（仕上げ）と同時** | UI-7, P9 |

### 9.4 v2.0 期

| UI-ID | UI項目 | 実装フェーズ |
|---|---|---|
| **UI-17** | 限界耐力：容量スペクトル・性能点の表示 | **P12 と同時** |
| **UI-18** | ML 断面提案UI（提案→規準検証ゲートの結果提示。未検証案は採用不可表示） | **P11 と同時** |
| **UI-19** | GPU 設定UI（概算モード切替・CPU確定の明示）任意 | **P10 と同時** |

### 9.5 各フェーズ仕様書への反映（タスク追加先）

上記 UI-x を、対応フェーズの**タスク一覧へ追加**する（実装者はそのフェーズに来たら UI-x も実装）:

| フェーズ仕様書 | 追加する UI タスク |
|---|---|
| [P3](P3_最小UIと設計.md) | UI-D1（T0 前提）, UI-1〜UI-7（T0〜T7 を本書 §1〜§7 の構成へ改訂） |
| [P0/squid-n-core](P0_基盤.md) | UI-D1 のデータ型（`SectionShape` 等）を §3.9 断面データに追記 |
| [P4](P4_材料と断面.md) | UI-8（配筋連携）, UI-9（断面タイプ追加） |
| [P1.5](P1.5_板要素.md) | UI-11（シェルコンター） |
| [P5](P5_非線形.md) | UI-10（ヒンジ・性能曲線） |
| [P6](P6_動的.md) | UI-12（時刻歴グラフ） |
| [P7](P7_二次設計.md) | UI-13（設計タブ拡充・幅厚比をShapeから） |
| [P8](P8_操作と連携.md) | UI-14（編集コマンド共通）, UI-15（CSV・ジョブ） |
| [P9](P9_仕上げ.md) | UI-16（レポート） |
| [P10/P11/P12] | UI-17〜UI-19 |

> **反映済み（実施完了）:** 上表のとおり、各フェーズ仕様書の「§1 タスク一覧」直後に UI-x のポインタを
> 追記済み。実装者は**自分が着手するフェーズの仕様書を見れば、そのフェーズでやる UI-x が分かる**。
> 完全な内容（式・データ型・DoD）は本書 §1〜§8 と §9.2〜§9.4 を参照。

---

## 10. DoD（UI 全体）

| # | 項目 | 章 |
|---|---|---|
| 1 | 4ペイン＋工程タブが動作（自由遷移・3D/ナビ/表 連動） | §1 |
| 2 | 各工程タブの入力・表示が揃う | §2 |
| 3 | 3D選択→断面編集（共有波及＋複製、Undo可） | §3 |
| 4 | 断面作成UI（全タイプ）→ 性能算定→ Section 生成 | §4 |
| 5 | stale 表示＋手動再計算（部分再計算） | §5 |
| 6 | 結果可視化（検定比色分け含む）が結果と一致 | §6 |
| 7 | 入力支援（コピペ・一括・即時検証） | §7 |
| 8 | レポート出力（PDF/Excel/CSV） | §8 |

---

## 11. 用語（UI で増えるもの）
| 用語 | 意味 |
|---|---|
| 工程タブ | 解析フロー順のタブ（モデル→…→レポート）。進行ロックしない |
| 4ペイン | 左ドック（ナビゲータ）＋中央(3D/表)＋右ドック（インスペクタ）＋下ドック（ログ）。各ドックはステータスバーのトグルで開閉可能 |
| ナビゲータ | 階/部材群/ケースのツリー（中央と連動） |
| インスペクタ | 右ペイン。選択要素の諸元・断面編集・検定サマリ |
| SectionShape | パラメトリック断面定義（形状＋寸法＋配筋）。Section を算定 |
| stale | モデル変更で結果/設計が古くなった状態（要再計算） |
| 共有編集 | 断面編集が同断面参照の全部材へ波及すること |
| 検定比色分け | 断面算定結果を 3D 上で緑/黄/赤に可視化 |

---

*本書はフェーズ横断の UI 全体設計。決定は §9 のとおり P3/P0/P4/P7/P8 へ反映する。*
*段階導入（P3 で土台→断面タイプ順次追加）を前提とする。*
